// eip-core — shared kernel (leaf module). No dependency on any other eip-* module
// (BackendPlan.md §1). Domain types + event envelope + SPIs land in TASK-0005 (P0-E2-S1).
plugins {
    id("eip.modulith-conventions")
}
