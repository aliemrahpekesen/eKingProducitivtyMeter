# TASK-0025 Handoff #1 — Debt-paydown sweep, paused mid-Wave-3b

- **Project:** `/Users/emrahpekesen/devenv/projects/productivity-meter`
- **Date paused:** 2026-07-21
- **Branch:** `feature/TASK-0017-local-install-profiles` (mirrored to `integration/SPRINT-00`)
- **Active goal-hook (verbatim):** "tüm debtleri tamamla" — session-scoped Stop hook; do not consider it satisfied until this file's remaining work is done and a final debt-status report is given.
- **Model note:** user switched the main-loop model to Sonnet 5 mid-session via `/model sonnet`. Standing directive unchanged: orchestrator (this loop) does specs/architecture/review/gates/commits; ALL code is written by Sonnet 5 subagents (Agent tool) with disjoint write-sets, independently re-reviewed/re-gated/live-verified before every commit — never trust an agent's own "gates pass" claim without re-running them yourself. This discipline caught several real bugs this session (see Gotchas).

## Completed and verified (through commit `0700728`, pushed, CI green on both push+PR runs)

1. **DEBT-001 residual** (eip-analytics catalog aliasing) + **DEBT-015** (enabled Dependabot vulnerability alerts → GitHub Dependency Graph API went 403→200; dropped `continue-on-error` from `ci.yml`'s dependency-review step) — done directly by orchestrator.
2. **Wave 1** (7 commits `a878bdf`…`1d1b8e7`): DEBT-006/008/014 doc reconciliation, DEBT-007 docs-lint extended to `/work` + 4 new L4 checks + L3 Roadmap reconciliation (found+fixed one real dangling link), DEBT-022 installer preprod secret generation (live-verified via scratch Postgres, dev stack untouched), DEBT-020 items 4-6 (TenantTransactionRunner nested-call guard, 3 real frontend null-vs-undefined bugs in FrictionCard, `ensureConnector` race closed with unique index + concurrent test).
3. **Wave 2** (commits `9b3f466`, `f665a23`, `f7f2614`): DEBT-018 residual (validate()/healthCheck() SPI additions, incrementalSupported flag, delete lifecycle for PR/review/build/quality-gate via V9), DEBT-016 (frontend OpenAPI→TS codegen, types.ts now a thin re-export). **Also fixed live-discovered bugs**: (a) `ConnectorAdminService#test/#health` let an unhandled `IllegalArgumentException` (stored config missing a required setting) escape as a raw 500 instead of RFC 7807 — fixed centrally, live-verified; (b) a real security investigation triggered by DEBT-015 actually blocking: vitest 2.1.8→3.2.7 (2 critical CVEs — RCE + arbitrary file read), vite 5.4.11→5.4.21, `brace-expansion`/`js-yaml` pinned to patched versions via `pnpm.overrides`. One finding (GHSA-fx2h-pf6j-xcff, vite `server.fs.deny` bypass, Windows-only/dev-server-only) is an **explicitly accepted, documented exception** via `allow-ghsas` in `ci.yml` — the fix needs vite 6.4.3+/7.x, which breaks `@vitejs/plugin-react`'s own peer range (tested, real unmet-peer conflict, not hypothetical). Revisit when that pairing stabilizes.
4. **Wave 3a** (commits `066fb0f`, `0700728`): DEBT-012 part 1 — service-token authentication (SecurityModel §3), mode-independent (works in both `header`/`oidc`), V10 migration. **Live-verified end to end**: create (VIEWER role) → use with spoofed `X-EIP-Tenant` (ignored, real tenant from token) → 200; role-scoped 403 on `report.generate`; revoke → 204 then 401; listing never exposes raw token/hash; wrong token → 401. **ADR-025** records a deliberate no-RLS design on `core.service_token` — I independently re-derived and endorsed this (the auth-lookup-by-hash path must see all tokens regardless of tenant to discover the tenant; no RLS predicate can scope it without breaking that lookup; mirrors `core.tenant`'s existing precedent). Governance sign-off (R-SA+R-DBA per DatabasePlan §2) is formally flagged as outstanding, not assumed. **New DEBT-024 registered** (not fixed — correctly out of scope): no `audit.audit_event` write path exists anywhere despite the table existing since V1 and SecurityModel §11 documenting the full taxonomy; service-token events are SLF4J-logged only. This is genuinely large (hash-chaining, verifier job, retrofitting every security action) — do not attempt it inside this sweep; it has its own debt row now.

## In progress — NOT committed — do this FIRST on resume

**Wave 3b (DEBT-010/011 — signed/expiring cursors + whitelisted sort + OpenAPI error docs + additive-diff CI gate)**: the background Sonnet agent (id `a78ef4fc1f3a095ed`) has now reported completion (its notification arrived AFTER the pause command — this update is the one exception the pause instructions allow: keeping the handoff file itself accurate; no code/gates/commits were touched in response to it). Its self-reported summary, NOT yet independently re-verified by the orchestrator:
- Cursor signing: identical HMAC-SHA256 scheme in both `ConnectorCursor` and `ReportCursor` (base64url over NUL-delimited payload + expiry + hmac; constant-time `MessageDigest.isEqual`; 1-hour TTL constant; generic rejection message, no oracle). New property `eip.api.cursor-signing-key` / env `EIP_API_CURSOR_SIGNING_KEY`, dev-fixture default added to `ProductionSecretsGuard`'s guarded set (extends the existing 3-var guard to 4). Cursor now embeds the issuing sort; a page-2 request with a different `sort` than page 1 → 400.
- Sort whitelist: `name` (default) | `-name` | `createdAt` | `-createdAt` on `/api/v1/connectors`.
- OpenAPI errors: a global `OperationCustomizer` (not per-endpoint) infers 400/401/403/404 from each handler's existing annotations, `$ref`-ing one shared `ProblemDetail` schema; 4 non-standard endpoints (ai-disabled/ai-upstream/ai-rejected/source-sync/webhook) got explicit `@ApiResponse`s. **Self-reported bug found+fixed along the way:** springdoc was silently dropping the 200 success response on 3 methods once any `@ApiResponse` was added — re-declared explicitly, verified no operation is left without a 2xx.
- CI: added an `EIP_OPENAPI_EXPORT=1` regenerate-and-diff step to the `backend` job (mirrors DEBT-016's frontend gate) — **this file's own visible current content already reflects that CI edit** (see `.github/workflows/ci.yml`'s "OpenAPI additive-only diff gate" step, ~line 56).
- Self-reported gates: full `./gradlew check` BUILD SUCCESSFUL (one real ArchUnit violation found+fixed: `io.swagger.v3.oas.annotations..` needed whitelisting for controllers), `jacocoTestCoverageVerification` all modules pass, `docs_lint.py` PASS over 202 files.
- Flagged deviations: `ReportCursor` reads its signing key via `System.getenv` directly (call site outside its write-set forced this); webhook's global-customizer-added 400 is a known minor imprecision (real failure modes are 401/404/429); `docs/architecture/generated/modulith/{components,module-app,module-reports}.puml` were regenerated as a side effect and kept (not reverted) — **verify whether this is real content or pure reordering churn before deciding, per this repo's established check.**

**NONE OF THE ABOVE HAS BEEN INDEPENDENTLY VERIFIED YET.** On resume, do exactly what every prior wave in this session required — do not skip steps just because the agent's own report sounds thorough:

`git status --short` at pause time showed:
```
M  backend/eip-app/src/main/java/com/eip/app/api/{AdminConnectorController,AiExplainController,ConnectorController,WebhookController}.java
M  backend/eip-app/src/main/java/com/eip/app/application/ListConnectorsQuery.java
M  backend/eip-app/src/main/java/com/eip/app/config/{OpenApiConfig,ProductionSecretsGuard}.java
M  backend/eip-app/src/main/java/com/eip/app/persistence/{ConnectorCursor,ConnectorQueryRepository}.java
M  backend/eip-app/src/test/java/com/eip/app/{TenantApiIntegrationTest,config/ProductionSecretsGuardTest}.java
M  backend/eip-reports/src/main/java/com/eip/reports/persistence/ReportCursor.java
M  config/environments/{dev,test,preprod,prod}.env
?? backend/eip-app/src/main/java/com/eip/app/config/ApiCursorSigningProperties.java
?? backend/eip-app/src/main/java/com/eip/app/persistence/ConnectorSort.java
?? backend/eip-app/src/test/java/com/eip/app/persistence/{ConnectorCursorTest,ConnectorSortTest}.java
?? backend/eip-reports/src/test/java/com/eip/reports/persistence/
```
No V11 migration exists — confirmed this is app-level HMAC signing, not a schema change (as expected).

**Steps to close Wave 3b:**
1. `git status --short` — confirm the tree still matches the list above (nothing else touched it).
2. Read the agent's stored output via the Read tool at `/private/tmp/claude-501/-Users-emrahpekesen-devenv-projects-productivity-meter/8b387f34-a32d-4fa3-850d-7e54e9722df1/tasks/a78ef4fc1f3a095ed.output` if still present (session-scratchpad may have been cleared — check first) — do NOT `cat` it via Bash, it's a large transcript.
3. Independently review: the HMAC signing scheme in `ConnectorCursor`/`ReportCursor` (constant-time compare via `MessageDigest.isEqual`, expiry check ordering, generic rejection message not leaking which check failed), the new `ApiCursorSigningProperties` key source (must be a NEW property, not reusing `EipSecretsProperties`' envelope-encryption master key), whether `ProductionSecretsGuard` was correctly extended to refuse prod/preprod boot on a dev-fixture/blank cursor-signing key (check `ProductionSecretsGuardTest`), the `ConnectorSort` whitelist, and the OpenAPI error-response documentation approach (per-endpoint `@ApiResponse` vs. a global customizer).
4. Run gates for real: `cd backend && ./gradlew check --console=plain` (use `--rerun-tasks` if anything looks suspiciously UP-TO-DATE), then `cd ../frontend && pnpm generate:api:check` (OpenAPI error-response docs may have changed the schema — regenerate if it drifts), then `python3 scripts/docs-lint/docs_lint.py` from repo root.
5. Live-verify: rebuild+restart the dev backend jar (command below), then confirm (a) a pagination round-trip still works end to end on `/api/v1/connectors`, (b) an old/unsigned or tampered cursor is rejected with 400, (c) requesting page 2 with a different `sort` value than page 1 is rejected, (d) booting with `SPRING_PROFILES_ACTIVE=prod` + a dev-fixture/blank cursor-signing key refuses boot naming that variable (mirror the `ProductionSecretsGuard` smoke pattern used for the other three guarded vars earlier this session), (e) the new CI additive-diff step actually catches a manufactured drift (edit the committed OpenAPI snapshot by one byte, confirm the gate fails, revert).
6. Check `docs/architecture/generated/modulith/*.puml` for real vs. reordering-only churn (`git diff docs/architecture/generated/modulith/ | grep "^[+-]Rel" | sort | uniq -c | awk '$1%2==1'` — empty means pure churn, `git checkout --` it).
7. Commit (probably 2 commits: backend cursor/OpenAPI work; config/environments changes — or one, your call after seeing the actual diff), `git push origin feature/TASK-0017-local-install-profiles`, then `git branch -f integration/SPRINT-00 HEAD && git push origin integration/SPRINT-00`, then `gh run list --limit 2` and `gh run watch <both-run-ids> --exit-status --interval 30` — watch the **PR-triggered** run specifically since it's the one carrying the dependency-review security gate, and this wave touches `ci.yml`.

## Remaining after Wave 3b closes

**Wave 4** (2 parallel agents, disjoint write-sets — assign explicit migration version numbers before launching, check the actual highest `V<N>` file first):
- **Agent A — DEBT-012 part 2:** per-tenant claim-mapping config (bounded v0.1: a deployment-level `eip.security.oidc.tenant-claim`/`roles-claim` property override, NOT truly per-tenant — there's a real chicken-egg problem discovering which tenant's config to use before the tenant is known from the claim itself; document this honestly, same reasoning pattern used elsewhere this session), resource-level team-scope checks (`EipPrincipal` gains an optional `scopedTeamIds` set from a claim; analytics/reports reads gain an optional team filter enforced for `ENGINEERING_MANAGER`/`TEAM_LEAD`/`RELEASE_MANAGER`), tenant-editable persona role-permission overrides (new `core.tenant_role_permission` override table, `PUT /api/v1/admin/roles/{role}/permissions` TENANT_ADMIN-only, effective permissions = default ∪/∖ overrides). Write-set: `eip-app` security/RBAC + `eip-tenancy` rbac + one new migration.
- **Agent B — DEBT-017 partitioning sub-item:** `staging.raw_*` weekly RANGE partitioning scaffolding (DatabasePlan §6) — concrete SQL migration + whatever repository adjustments are needed. This is ONLY the partitioning schema — NOT the "100k events/h sustained" proof, which needs reference hardware and stays honestly undemonstrated (already covered by the existing `docs/performance/G5-Evidence-SPRINT-01.md`). Write-set: `eip-app` migrations (a DIFFERENT version number than Agent A's) + `eip-ingestion` staging repository.

After Wave 4 lands: same discipline — independent review, full gates, live verification, commit, push both branches, watch both CI runs.

**Final debt-register.md pass:** mark every closed item RESOLVED with evidence pointers (match the tone/format already used for DEBT-001/006/007/008/012/014/015/018/020/022 rows). Leave these explicitly OPEN with their existing justification — do NOT force-close, they are correctly deferred, not oversights:
- **DEBT-005 residual** — waits on an eip-app/eip-migrate Compose-container architecture decision not requested.
- **DEBT-021 residual** — full AI-composed-report job-runner/MinIO/LangChain4j; ADR-024 deliberately defers to a distinct future agent-runtime phase.
- **DEBT-023** — depends on an agent runtime that does not exist yet.
- **DEBT-024** — audit_event write path; registered this session, correctly out of scope (too large for a debt-paydown sweep, needs its own dedicated task).

**Final report to the user:** once every wave is done/gated/verified/committed/pushed/CI-green, produce the closing summary in the same "fonksiyon matrisi + audit matrisi + kalan işler planı" format used at the end of every prior milestone this session, explicitly restating why the four items above stay open (don't re-litigate, just restate the existing reasoning) — only then does the "tüm debtleri tamamla" goal-hook condition genuinely hold.

## Resume commands

```bash
# From repo root: /Users/emrahpekesen/devenv/projects/productivity-meter
git status --short && git log --oneline -3          # confirm tree state matches this handoff

# Backend gate (always re-run for real; use --rerun-tasks if results look cached)
cd backend && ./gradlew check --console=plain

# Frontend gates
cd frontend && pnpm -s test && pnpm -s lint && pnpm -s typecheck && pnpm -s build && pnpm -s generate:api:check

# Docs-lint (from repo root)
python3 scripts/docs-lint/docs_lint.py

# Rebuild + restart dev backend (from repo root)
kill $(cat .install/backend.pid) 2>/dev/null
(cd backend && ./gradlew -q :eip-app:bootJar)
JAR="$(ls backend/eip-app/build/libs/*.jar | grep -v -- '-plain' | head -1)"
EIP_DB_URL="jdbc:postgresql://localhost:5432/eip" EIP_APP_DB_USER="eip_app" \
  EIP_APP_DB_PASSWORD="eip_app_dev_pw" EIP_MIGRATOR_DB_USER="eip" \
  EIP_MIGRATOR_DB_PASSWORD="eip_dev_pw" EIP_OTLP_TRACES_ENDPOINT="http://localhost:4318/v1/traces" \
  SERVER_PORT=8080 SPRING_PROFILES_ACTIVE=dev nohup java -jar "$JAR" > .install/backend.log 2>&1 &
echo $! > .install/backend.pid

# Demo tenant for live checks: 00000000-0000-4000-8000-0000000000de (header X-EIP-Tenant)

# Push pattern used all session
git push origin feature/TASK-0017-local-install-profiles
git branch -f integration/SPRINT-00 HEAD && git push origin integration/SPRINT-00
gh run list --limit 2
gh run watch <run-id> --exit-status --interval 30   # watch BOTH push and PR runs
```

## Running services at pause time

- Backend: pid in `.install/backend.pid` (was `84207`, likely stale after restarts — re-check), port 8080, health 200 — but this is the Wave-3a jar, NOT rebuilt with Wave 3b's uncommitted changes.
- Frontend: vite dev server, port 5173, health 200 — untouched by Wave 3b (backend/reports-only changes).
- Docker infra (`eip-dev-*` named containers): postgres, kafka, redis, keycloak, minio, otel-collector, prometheus, grafana — all running, all owned by this project.
- **Do not touch**: a few stray non-`eip-dev-*` containers were visible (`hermes-verify`, `quizzical_chaum`, `nice_villani`, `vigorous_merkle`, `hardcore_clarke`) — unclear ownership, unrelated to this project, do not assume you may stop/remove them.
- Flyway is at V10 (`service_token`) — confirm this hasn't changed before assuming Wave 3b needs no migration.

## Gotchas / decisions to preserve

- **Never trust an agent's own gate-pass claim** — always independently re-run `./gradlew check`/frontend gates/docs-lint yourself before committing. This caught: the ConnectorAdminService 500 bug, migration-numbering coordination needs, and the vitest/vite CVEs (surfaced by re-running the dependency-review gate for real, not by any agent's self-report).
- **`work/debt-register.md` is single-writer** — only the orchestrator (not background agents) edits it, at the end of each wave, reconciling any inline notes agents left.
- **Puml churn**: `docs/architecture/generated/modulith/*.puml` regenerates on every test run; check for real edges vs. pure reordering before deciding whether to keep or revert (`git checkout --` the pure-churn case).
- **PR #1 (integration→main) stays unmerged** — user explicitly deferred this ("merge sonraki iş olsun"); it is a separate task from this debt sweep, do not merge it as a side effect.
- **AI layer (M6/ADR-024) is fully shipped and out of scope for this sweep** — deterministic-first, opt-in, explains-only; don't regress it, don't extend it here.
- **Migration numbering**: when Wave 4 launches two parallel agents that might both want the "next" migration file, assign explicit version numbers in each agent's prompt after checking the real highest `V<N>` file yourself — do not let two parallel agents guess the same number.

## First step on resume

Re-read this file's "In progress" section, run `git status --short` to confirm the tree matches, then execute the 7-step Wave-3b closeout list above (review → gate → live-verify → commit → push → watch both CI runs) before touching anything else.
