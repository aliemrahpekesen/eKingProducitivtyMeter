# Technical Debt Register

Register-or-fix rule ([TechnicalDebtPolicy](../engineering-operating-system/TechnicalDebtPolicy.md)): every accepted shortcut is a `DEBT-NNN` entry here. Fields: id · origin · description · risk if unpaid · size · target phase/sprint · owner. Items older than two phases escalate to R-CA.

| ID | Origin | Description | Risk if unpaid | Size | Target | Owner |
|---|---|---|---|---|---|---|
| DEBT-001 | TASK-0001 (MINOR-1); TASK-0005 (NIT-2) | `backend/gradle/libs.versions.toml` claims to be the single source of truth for backend versions, but versions are duplicated as string literals in `backend/buildSrc/build.gradle.kts` (spring-boot `3.4.1`, dependency-management `1.1.7`, spotless `6.25.0`), the foojay resolver `0.8.0` is hardcoded in `settings.gradle.kts`, and module build files use string coordinates instead of catalog accessors. **TASK-0005 partially advanced this** (added `jspecify`/`assertj` as proper catalog entries) but left one new string coordinate — `testImplementation("org.springframework.modulith:spring-modulith-docs")` in `backend/eip-core/build.gradle.kts` (BOM-versioned; NIT-2) — which should get a catalog alias. Several catalog entries remain unreferenced. | Version drift: a catalog bump silently fails to propagate to `buildSrc`/settings; two sources of truth diverge. Grows as more dependencies land. | S | SPRINT-01 — dedicated catalog-consistency pass (the "fold into TASK-0005" target is now closed; TASK-0005 merged without fully paying this): route `buildSrc`, `settings.gradle.kts`, and remaining module string coordinates (incl. `spring-modulith-docs`) through the catalog | R-BA |
| DEBT-002 | TASK-0001 (MINOR-2) | `backend/gradle/verification-metadata.xml` (Gradle dependency checksum verification) is absent, though [RepositoryStructure §2](../engineering-operating-system/RepositoryStructure.md) lists it in the canonical backend build scaffolding (a [DependencyManagement §10](../engineering-operating-system/DependencyManagement.md) supply-chain control). | No checksum verification on dependency downloads — supply-chain integrity gap for an air-gap-mirrored build (NFR-051). Deferred deliberately until the dependency set stabilizes. | S | SPRINT-00/01 (fold into TASK-0002 supply-chain wiring or a dedicated DependencyManagement task once deps stabilize) | R-DOA (R-SA: supply-chain) |
| DEBT-003 | TASK-0002 (MINOR-1) | The JaCoCo coverage ratchet in `backend/buildSrc/src/main/kotlin/eip.java-conventions.gradle.kts` special-cases only `eip-core` at ≥85% and uses JaCoCo's default INSTRUCTION counter. [TestingStrategy §1](../docs/testing/TestingStrategy.md) requires ≥85% **line** / ≥75% **branch** on `eip-core` **and `eip-analytics`**, ≥75% line elsewhere. | Under-enforces coverage once `eip-analytics` and branch coverage matter; the ratchet does not match the source-of-record semantics. No impact while modules are empty. | S | SPRINT-04 (before `eip-analytics` code lands in Phase 2) — add `eip-analytics` to the ≥85% tier and express separate `LINE` and `BRANCH` limits | R-QAA (R-BA: build config) |
| DEBT-004 | TASK-0002 (MINOR-3) | Prettier formatting check is not wired for the frontend, though [CodingStandards §1](../engineering-operating-system/CodingStandards.md) lists "Prettier check + ESLint" for TS formatting/lint. Only ESLint + `tsc` are wired (the TASK-0002 Static stage enumerated "ESLint/tsc"). | TS formatting is unenforced at G1 until the frontend has real code; low risk while the shell is a placeholder. | S | SPRINT-03 (fold into the frontend-shell task P0-E5-S1, which brings the real UI + design tokens) | R-FA |
| DEBT-005 | TASK-0003 (MINOR-3) | `infra/docker-compose/.env.example` ships working dev values (`eip_dev_pw`, `admin_dev_pw`, …) rather than the [DockerCompose §1.1](../docs/infrastructure/DockerCompose.md) `CHANGE_ME` convention (secret-bearing vars ship as `CHANGE_ME`; `eip-app` refuses to start on `CHANGE_ME`). Acceptable for the dev-infra-only stack (boots out of the box; `.env` git-ignored; non-production), but it must converge with §1.1 once the app container is added so prod operators are forced to set real secrets. | A prod install could inherit weak default credentials if the `CHANGE_ME` refuse-to-start guard is absent when the app container arrives. No risk while the stack is infra-only in dev. | S | The task that adds the `eip-app`/`eip-migrate` Compose containers (Phase 0/1) — introduce `CHANGE_ME` for secret-bearing vars + the app-side config-validation guard | R-DOA (R-SA: secrets) |
| DEBT-006 | TASK-0007 (MINOR-1) | `.github/CODEOWNERS` adds rules not present in [ModuleOwnership §1/§4](../engineering-operating-system/ModuleOwnership.md) (`/program`, `/sprints`, `/reviews`, loose root files `.editorconfig`/`.gitignore`/`CONTRIBUTING.md`/`LICENSE`, the `*` default, `/.github/CODEOWNERS`) and lives at `.github/` while §4 note 3 / [RepositoryRules §7.1](../engineering-operating-system/RepositoryRules.md) say the repo root. All additions are disclosed and correct per §1 principles + §6.3, and R-CA ratified the `.github/` location — but the source-of-record map (ModuleOwnership §1/§4) does not yet reflect them. | [ModuleOwnership §7](../engineering-operating-system/ModuleOwnership.md) makes CODEOWNERS↔§1 role/path drift a **G7 docs-lint failure**; once TASK-0008's docs-lint activates on the remote, the §1↔CODEOWNERS subset check (if wired) or a manual G7 pass would flag the drift. No functional risk locally (docs-lint not yet remote-active). | S | Before TASK-0008 docs-lint activates on the remote — a CC-6 R-CA doc PR updating ModuleOwnership §1/§4 (add the post-§4 rows + `*` default) and RepositoryRules §7.1/§7 + §4 note 3 wording ("root" → `.github/`) so map and artifact reconverge | R-CA |
