# Technical Debt Register

Register-or-fix rule ([TechnicalDebtPolicy](../engineering-operating-system/TechnicalDebtPolicy.md)): every accepted shortcut is a `DEBT-NNN` entry here. Fields: id · origin · description · risk if unpaid · size · target phase/sprint · owner. Items older than two phases escalate to R-CA.

| ID | Origin | Description | Risk if unpaid | Size | Target | Owner |
|---|---|---|---|---|---|---|
| DEBT-001 | TASK-0001 (MINOR-1); TASK-0005 (NIT-2) | `backend/gradle/libs.versions.toml` claims to be the single source of truth for backend versions, but versions are duplicated as string literals in `backend/buildSrc/build.gradle.kts` (spring-boot `3.4.1`, dependency-management `1.1.7`, spotless `6.25.0`), the foojay resolver `0.8.0` is hardcoded in `settings.gradle.kts`, and module build files use string coordinates instead of catalog accessors. **TASK-0005 partially advanced this** (added `jspecify`/`assertj` as proper catalog entries) but left one new string coordinate — `testImplementation("org.springframework.modulith:spring-modulith-docs")` in `backend/eip-core/build.gradle.kts` (BOM-versioned; NIT-2) — which should get a catalog alias. Several catalog entries remain unreferenced. | Version drift: a catalog bump silently fails to propagate to `buildSrc`/settings; two sources of truth diverge. Grows as more dependencies land. | S | SPRINT-01 — dedicated catalog-consistency pass (the "fold into TASK-0005" target is now closed; TASK-0005 merged without fully paying this): route `buildSrc`, `settings.gradle.kts`, and remaining module string coordinates (incl. `spring-modulith-docs`) through the catalog | R-BA |
| DEBT-002 | TASK-0001 (MINOR-2) | `backend/gradle/verification-metadata.xml` (Gradle dependency checksum verification) is absent, though [RepositoryStructure §2](../engineering-operating-system/RepositoryStructure.md) lists it in the canonical backend build scaffolding (a [DependencyManagement §10](../engineering-operating-system/DependencyManagement.md) supply-chain control). | No checksum verification on dependency downloads — supply-chain integrity gap for an air-gap-mirrored build (NFR-051). Deferred deliberately until the dependency set stabilizes. | S | SPRINT-00/01 (fold into TASK-0002 supply-chain wiring or a dedicated DependencyManagement task once deps stabilize) | R-DOA (R-SA: supply-chain) |
| DEBT-003 | TASK-0002 (MINOR-1) | The JaCoCo coverage ratchet in `backend/buildSrc/src/main/kotlin/eip.java-conventions.gradle.kts` special-cases only `eip-core` at ≥85% and uses JaCoCo's default INSTRUCTION counter. [TestingStrategy §1](../docs/testing/TestingStrategy.md) requires ≥85% **line** / ≥75% **branch** on `eip-core` **and `eip-analytics`**, ≥75% line elsewhere. | Under-enforces coverage once `eip-analytics` and branch coverage matter; the ratchet does not match the source-of-record semantics. No impact while modules are empty. | S | SPRINT-04 (before `eip-analytics` code lands in Phase 2) — add `eip-analytics` to the ≥85% tier and express separate `LINE` and `BRANCH` limits | R-QAA (R-BA: build config) |
| DEBT-004 | TASK-0002 (MINOR-3) | Prettier formatting check is not wired for the frontend, though [CodingStandards §1](../engineering-operating-system/CodingStandards.md) lists "Prettier check + ESLint" for TS formatting/lint. Only ESLint + `tsc` are wired (the TASK-0002 Static stage enumerated "ESLint/tsc"). | TS formatting is unenforced at G1 until the frontend has real code; low risk while the shell is a placeholder. | S | SPRINT-03 (fold into the frontend-shell task P0-E5-S1, which brings the real UI + design tokens) | R-FA |
| DEBT-005 | TASK-0003 (MINOR-3) | `infra/docker-compose/.env.example` ships working dev values (`eip_dev_pw`, `admin_dev_pw`, …) rather than the [DockerCompose §1.1](../docs/infrastructure/DockerCompose.md) `CHANGE_ME` convention (secret-bearing vars ship as `CHANGE_ME`; `eip-app` refuses to start on `CHANGE_ME`). Acceptable for the dev-infra-only stack (boots out of the box; `.env` git-ignored; non-production), but it must converge with §1.1 once the app container is added so prod operators are forced to set real secrets. | A prod install could inherit weak default credentials if the `CHANGE_ME` refuse-to-start guard is absent when the app container arrives. No risk while the stack is infra-only in dev. | S | The task that adds the `eip-app`/`eip-migrate` Compose containers (Phase 0/1) — introduce `CHANGE_ME` for secret-bearing vars + the app-side config-validation guard | R-DOA (R-SA: secrets) |
| DEBT-006 | TASK-0007 (MINOR-1) | `.github/CODEOWNERS` adds rules not present in [ModuleOwnership §1/§4](../engineering-operating-system/ModuleOwnership.md) (`/program`, `/sprints`, `/reviews`, loose root files `.editorconfig`/`.gitignore`/`CONTRIBUTING.md`/`LICENSE`, the `*` default, `/.github/CODEOWNERS`) and lives at `.github/` while §4 note 3 / [RepositoryRules §7.1](../engineering-operating-system/RepositoryRules.md) say the repo root. All additions are disclosed and correct per §1 principles + §6.3, and R-CA ratified the `.github/` location — but the source-of-record map (ModuleOwnership §1/§4) does not yet reflect them. | [ModuleOwnership §7](../engineering-operating-system/ModuleOwnership.md) makes CODEOWNERS↔§1 role/path drift a **G7 docs-lint failure**; once TASK-0008's docs-lint activates on the remote, the §1↔CODEOWNERS subset check (if wired) or a manual G7 pass would flag the drift. No functional risk locally (docs-lint not yet remote-active). | S | Before TASK-0008 docs-lint activates on the remote — a CC-6 R-CA doc PR updating ModuleOwnership §1/§4 (add the post-§4 rows + `*` default) and RepositoryRules §7.1/§7 + §4 note 3 wording ("root" → `.github/`) so map and artifact reconverge | R-CA |
| DEBT-007 | TASK-0008 (MINOR-1/2/3) | The docs-lint implementation (`scripts/docs-lint/docs_lint.py`) covers the four [DocumentationStandards §4](../engineering-operating-system/DocumentationStandards.md) check *types* but not their full breadth: (1) `/work` is out of scope, so the L1/L2 `/work` + `TASK`/`SPRINT`/`DEBT`/`RISK` id resolution DocumentationStandards §4 specifies is not enforced; (2) the L4 canonical seed enforces only the RLS-GUC value, not the DLQ pattern, NFR-figure drift, `en-US` locale, or `pnpm`; (3) L3 reconciles FeatureCatalog only, not Roadmap stated totals. | Incomplete drift coverage: broken links/dangling ids inside `/work`, and non-GUC canonical drift (DLQ, NFR figures, locale, pnpm) or Roadmap count drift, are not caught at G7 until the checks are extended. Low risk while the doc set is small and freshly reviewed. | M | When docs-lint goes remote-live and the doc set stabilizes — extend scope to `/work` (with a "defined includes planned" model for future TASK/SPRINT ids), add context-aware L4 patterns for DLQ/NFR/locale/pnpm, and add Roadmap L3 reconciliation (R-DE owns the L4 grep list per DocumentationStandards §4) | R-DE + R-DOA |
| DEBT-008 | TASK-0009 (MINOR-1) | [DatabasePlan §14](../docs/engineering/DatabasePlan.md) AC-1 reads "V1__baseline.sql creates all §2 schemas **and §3 tables**", but §7 also says "V2+ begins with Phase 1 connector additions" — an internal tension. TASK-0009 built V1 as the Phase-0 **control-plane** baseline (all 11 §2 schemas + tenancy/security/connector/audit/analytics-metric tables) and deferred the connector-populated canonical business tables (`scm`/`cicd`/`quality`/`ops` detail) and Phase-3 RAG/agent/report tables to their features (product-first; avoids premature unused schema). Documented in the task file and R-DBA-ratified, but §7/§14 wording still says "full Phase-0 schema in V1". | Wording drift between DatabasePlan §7/§14 and the as-built incremental V1; a future reader could expect all canonical tables in V1. No functional gap (tables land with the features that populate them, expand-only). | S | A CC-6 R-DBA + R-DE doc PR clarifying §7/§14 that V1 is built incrementally — control-plane first, connector-populated canonical tables with their Phase-1 tasks, RAG/agent/report with Phase-3 (expand-only). Land before or with the first Phase-1 connector task | R-DBA + R-DE |
| DEBT-009 | TASK-0010 (MAJOR-1; MINOR-1); TASK-0013 | **✅ RESOLVED by TASK-0012.** (Was: G6 observability waiver.) The `/api/v1` read endpoints (`session`, `connectors`, `friction/summary`) shipped with no Micrometer `eip_*` metrics, no OTel spans, and no structured JSON logs, and the problem+json body carried no `traceId` ([BackendPlan §10](../docs/engineering/BackendPlan.md)). [QualityGatePolicy line 119](../engineering-operating-system/QualityGatePolicy.md) makes G6 content-triggered (adds endpoints); [line 81] permits deferral only via an R-OE DEBT waiver. `traceId` derives from the OTel/`traceparent` MDC that the observability wiring populates ([BackendPlan §10 line 86](../docs/engineering/BackendPlan.md): "populated by the tenant-context filter and OTel instrumentation"). | No metrics/traces/logs on the first live HTTP surface: request rate, latency, and 401/400 rates are invisible; incidents on these endpoints are undebuggable; problem responses lack a correlation id. Grows as endpoints multiply. | ~~S~~ **DONE** | **TASK-0012** delivered: actuator health/info/prometheus, `eip_api_request_duration_seconds` + `eip_api_requests_inflight` (tenant-safe tags), OTel tracing → Compose collector, structured JSON logs (`tenantId`/`traceId`/`spanId` MDC), and `traceId` on every problem+json. Dashboards/alerts remain in `/infra` (ObservabilityModel §5/§6). | R-OE |
| DEBT-010 | TASK-0010 (MINOR-8) | **`/api/v1` pagination hardening.** The `/connectors` list envelope (`PageView`) and keyset cursor are correct and honored (`limit` default 50/max 200, opaque `(name,id)` cursor, `hasMore`), but two APIDesign §1.4 aspects are not yet met: the cursor is **not signed and does not expire** (`ConnectorCursor` is plain base64url), and the **`sort` param (whitelisted fields)** is not implemented (fixed `ORDER BY name, id`). The client-facing contract (opaque cursor string + envelope) is stable, so both are additive to add later. | An unsigned cursor can be hand-crafted/tampered by clients (low impact — it only shifts the keyset within the caller's own RLS-scoped rows, never across tenants); no `sort` flexibility. Low risk while lists are small and single-sort. | S | **TASK-0011** (OpenAPI/pagination hardening) or the first task needing multi-sort/large lists — sign+expire cursors (per APIDesign §1.4) and add the whitelisted `sort` param. | R-BA |
| DEBT-011 | TASK-0010 (MINOR-4/5/6; NIT-2) | **OpenAPI contract completeness + drift-verification + seeder test.** (1) The committed snapshot `backend/eip-app/openapi/eip-openapi-v1.json` documents only 200 responses — the 401/400 `application/problem+json` error shapes are undocumented. (2) The snapshot is regenerated only under `EIP_OPENAPI_EXPORT` and is **not verified against the generated contract in CI**, so it can silently drift. (3) `DemoDataSeeder` (AC5 — real idempotency + RLS-bind logic) has no test and is coverage-excluded via `**/config/**`. | Clients can't see the error contract; a stale committed snapshot passes CI unnoticed; the demo seeder could regress unobserved. Low risk while the surface is tiny and freshly reviewed. | M | **TASK-0011** (OpenAPI lane): document error responses (`@ApiResponse` / global customizer), wire the additive-only OpenAPI diff gate (G1) that fails on committed↔generated drift, and add a `demo`-profile seeder test (or move it out of the `config/**` coverage exclusion). | R-BA |
| DEBT-012 | TASK-0010 (MINOR-7) | **Endpoint RBAC + denied-path tests.** `GET /api/v1/session` and `/api/v1/connectors` declare no RBAC permission and have no denied-path (403) test, which [DefinitionOfDone line 45](../engineering-operating-system/DefinitionOfDone.md) requires for every new endpoint (FR-122; CLAUDE Law 5). Spring Security is not yet on the eip-app classpath — auth/authz (OIDC + deny-by-default RBAC) is the SPRINT-02 scope (P0-E3-S3) that also replaces the dev `HeaderTenantResolver`. Tenant isolation (RLS) is already enforced and proven; this is the *authorization* layer on top. Applies to `GET /api/v1/friction/summary` (TASK-0013) too. | Endpoints are gated only by the dev tenant header until SPRINT-02; no permission enforcement or denied-path coverage. Acceptable only until the security filter chain lands (no prod profile exists yet; `HeaderTenantResolver` is `@Profile("!prod")`). | M | **SPRINT-02** (P0-E3-S3 OIDC/RBAC): declare permissions per endpoint in the eip-tenancy catalog, enforce deny-by-default, and add denied-path tests. | R-BA (R-PA: RBAC) |
| DEBT-013 | TASK-0013 | **Friction index computed in the API read path, not the analytics engine.** `GET /api/v1/friction/summary` derives each team's score with `com.eip.app.api.FrictionScore` over the `analytics.rm_team_flow_current` read model at request time. The architecture (deterministic engine = source of truth; ADR-015 read models) puts metric compute in `eip-analytics`, projected to `analytics.metric_fact` (a time series), with the API reading stored facts. This v1 placeholder also isn't the real Friction Index — the hero metric is wait-time decomposition across the correlated flow graph (needs the ingestion→canonical→correlation pipeline). | Metric logic lives in the composition root instead of the analytics module; no `metric_fact` history/trends; the placeholder score is not the differentiating wait-time metric. Low risk while there is no analytics engine and the score is transparent + deterministic + team-level; must move before real friction compute lands. | M | **Phase-2 analytics** (when `eip-analytics` is built): move friction compute into the deterministic engine, project to `metric_fact`, and have the API read stored facts; implement the real wait-time-decomposition Friction Index. | R-AE (R-CA: contract) |
