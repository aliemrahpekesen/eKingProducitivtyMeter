# Technical Debt Register

Register-or-fix rule ([TechnicalDebtPolicy](../engineering-operating-system/TechnicalDebtPolicy.md)): every accepted shortcut is a `DEBT-NNN` entry here. Fields: id · origin · description · risk if unpaid · size · target phase/sprint · owner. Items older than two phases escalate to R-CA.

| ID | Origin | Description | Risk if unpaid | Size | Target | Owner |
|---|---|---|---|---|---|---|
| DEBT-001 | TASK-0001 (MINOR-1); TASK-0005 (NIT-2) | **✅ RESOLVED by M5 Wave S2 (TASK-0023) catalog-consistency pass.** (Was: `backend/gradle/libs.versions.toml` claimed single-source-of-truth status but versions were duplicated as string literals in `backend/buildSrc/build.gradle.kts`, the foojay resolver was hardcoded in `settings.gradle.kts`, and module build files used raw string coordinates.) `buildSrc/settings.gradle.kts` now wires `backend/gradle/libs.versions.toml` as buildSrc's own `libs` version catalog (`dependencyResolutionManagement.versionCatalogs` — the established Gradle idiom for a separate buildSrc build); `buildSrc/build.gradle.kts`'s plugin-classpath dependencies (spring-boot-gradle-plugin, dependency-management-plugin, spotless-plugin-gradle, gradle-errorprone-plugin) route through new catalog library aliases. The precompiled convention plugins (`eip.java-conventions.gradle.kts`, `eip.modulith-conventions.gradle.kts`) route checkstyle/jacoco `toolVersion`, errorprone/nullaway dependency versions, the Java toolchain version, and Google Java Format's version through the same catalog via Gradle's documented `VersionCatalogsExtension` API — confirmed empirically that the type-safe `libs` accessor is unavailable inside a precompiled script plugin's own file (`:buildSrc:compileKotlin`/`:buildSrc:compilePluginsBlocks` fail "Unresolved reference: libs" for both its `plugins {}` block and its body), so the programmatic API is the correct idiom here, not a workaround. Module build files (`eip-core`, `eip-tenancy`, `eip-connectors`, `eip-ingestion`, `eip-reports`, `eip-app`) now alias `spring-modulith-core`/`-docs`, `jackson-databind`/`-datatype-jsr310`, `spring-context`/`-jdbc`/`-tx`, `postgresql`, `flyway-core`/`-database-postgresql`, `testcontainers-postgresql`/`-kafka`, `archunit-junit5`, `wiremock-standalone` (new `wiremock` version entry), `junit-jupiter`, and the pre-existing (previously unreferenced) `spring-boot-starter` alias. Every catalog entry is now referenced except two genuinely irreducible residuals, documented in place: (1) `settings.gradle.kts`'s own top-level `plugins {}` block (foojay-resolver-convention) — a settings script's own plugins block resolves before that same file's version-catalog machinery exists, so it cannot reference the catalog at all; (2) the `[plugins]` catalog entries for spring-boot/spring-dependency-management/spotless are not referenced via `alias(libs.plugins.x)` inside buildSrc's own precompiled `plugins {}` blocks, for the identical isolated-compilation reason as (1) — their versions are still single-sourced via the corresponding LIBRARY aliases in `buildSrc/build.gradle.kts`, so no version literal is actually duplicated, only the id-application syntax stays a bare `id(...)`. `eip-analytics/build.gradle.kts` was intentionally left untouched — owned by the parallel M5 Wave S3 agent per this task's write-set boundary — and still carries the same class of unaliased BOM-managed string coordinates the other modules had before this pass; a follow-up should apply the identical aliases there. `eip-ai`/`eip-workers` had no literal coordinates to route (project-only dependencies). | ~~Version drift risk~~ closed for buildSrc + all modules but eip-analytics. | ~~S~~ **Done** (residual: `eip-analytics` catalog aliasing, opportunistic) | Apply the same catalog aliases to `eip-analytics/build.gradle.kts` in a task that owns that module's write-set. | R-BA |
| DEBT-002 | TASK-0001 (MINOR-2) | **✅ RESOLVED by M5 Wave S2 (TASK-0023).** (Was: `backend/gradle/verification-metadata.xml` absent, though [RepositoryStructure §2](../engineering-operating-system/RepositoryStructure.md) lists it in the canonical backend build scaffolding.) Generated via `./gradlew --write-verification-metadata sha256 help` then `./gradlew --write-verification-metadata sha256 check` (captures test-only/plugin-resolution deps too — 596 verified components); a subsequent clean `./gradlew check --no-build-cache` (verification active automatically once the file exists — no extra flag) passed with **BUILD SUCCESSFUL**, no trusted-key or global-disable escapes added. | ~~No checksum verification on dependency downloads~~ closed — supply-chain integrity gap for an air-gap-mirrored build (NFR-051) now covered. | ~~S~~ **Done** | — | R-DOA (R-SA: supply-chain) |
| DEBT-003 | TASK-0002 (MINOR-1) | **✅ RESOLVED by M5 Wave S2 (TASK-0023).** (Was: the JaCoCo coverage ratchet in `backend/buildSrc/src/main/kotlin/eip.java-conventions.gradle.kts` special-cased only `eip-core` at ≥85% using JaCoCo's default INSTRUCTION counter, not [TestingStrategy §1](../docs/testing/TestingStrategy.md)'s ≥85% line / ≥75% branch on `eip-core` **and `eip-analytics`**, ≥75% line elsewhere.) The ratchet now expresses explicit `LINE`/`BRANCH` `JacocoCoverageVerification` limits per TestingStrategy §1's exact tiers. **Measured before any threshold change** (fresh `jacocoTestReport` XML, `./gradlew check --no-build-cache`): `eip-core` 100.0% line/100.0% branch; `eip-tenancy` 96.73% line; `eip-connectors` 88.58% line; `eip-ingestion` 81.46% line; `eip-analytics` 98.82% line/89.58% branch; `eip-reports` 90.7% line; `eip-app` 91.63% line — every non-empty module already cleared its doc-mandated tier, so **zero test-writing was needed**; only the ratchet's configuration was wrong. `eip-ai`/`eip-workers` remain source-empty (`jacocoTestCoverageVerification` SKIPPED by Gradle) — the rule activates automatically the moment they gain code. `./gradlew jacocoTestCoverageVerification` passes clean across every module with the corrected thresholds. | ~~Under-enforces coverage~~ closed. | ~~S~~ **Done** | — | R-QAA (R-BA: build config) |
| DEBT-004 | TASK-0002 (MINOR-3) | **✅ RESOLVED by TASK-0014 (P0-E5-S1).** (Was: Prettier formatting check not wired for the frontend, though [CodingStandards §1](../engineering-operating-system/CodingStandards.md) lists "Prettier check + ESLint".) Prettier is now wired: `.prettierrc.json` + `format`/`format:check` scripts, `eslint-config-prettier` to avoid rule conflicts, and a `pnpm format:check` step in the `frontend` CI job (G1). | ~~TS formatting unenforced at G1~~ closed. | S | ~~SPRINT-03~~ **Done** in TASK-0014. | R-FA |
| DEBT-005 | TASK-0003 (MINOR-3) | **✅ RESOLVED for the `config/environments/*.env` + app-guard scope by M5 Wave S2 (TASK-0023, DEBT-005).** (Was: no `CHANGE_ME` convention or app-side boot refusal existed for secret-bearing install config.) `config/environments/prod.env` and `preprod.env` now ship `EIP_APP_DB_PASSWORD`/`EIP_MIGRATOR_DB_PASSWORD`/`EIP_SECRETS_MASTER_KEY` as `CHANGE_ME` with an operator comment block (prod already had DB/OIDC `CHANGE_ME`; preprod gained the same three, previously left at defaults). New `ProductionSecretsGuard` (`com.eip.app.config`, registered in `META-INF/spring.factories` alongside `ProductionTenantResolutionGuard`) refuses `prod`/`preprod` boot with ONE aggregated message naming (never valuing) any of those three env vars left blank/unset, `CHANGE_ME`, or a known dev/demo fixture value (`eip_dev_pw`, `eip_app_dev_pw`, `admin_dev_pw`, `eip_minio_dev_pw`, `EipSecretsProperties.DEV_ONLY_MASTER_KEY`) — proven by `ProductionSecretsGuardTest` (10 cases: CHANGE_ME/dev-fixture/blank/unset all fail per-profile with the right var named and no leaked value; real values pass; dev/test/demo profiles untouched regardless of value). `infra/docker-compose/.env.example` (the ORIGINAL DEBT-005 target — Compose infra dev credentials) is unchanged and **remains open**: it's still not in scope until the `eip-app`/`eip-migrate` Compose containers land, per the original target below. **New consequence discovered and tracked separately:** `scripts/install/install.sh --env preprod` now fails to boot — see DEBT-022. | ~~No CHANGE_ME convention or refusal guard for install-config secrets~~ closed for `config/environments/*.env`. Compose `.env.example` risk unchanged (still infra-only, non-production, `.env` git-ignored). | S | Residual: `infra/docker-compose/.env.example` CHANGE_ME convergence still waits on the `eip-app`/`eip-migrate` Compose containers landing (Phase 0/1) | R-DOA (R-SA: secrets) |
| DEBT-006 | TASK-0007 (MINOR-1) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep).** (Was: `.github/CODEOWNERS` added rules not present in ModuleOwnership §1/§4 and lived at `.github/` while §4 note 3 / RepositoryRules §7.1 said the repo root.) [ModuleOwnership §1/§4](../engineering-operating-system/ModuleOwnership.md) now correctly name `.github/CODEOWNERS`'s actual location and carry the rows the artifact already had (`/program`, `/sprints`, `/reviews`, loose root files, the `*` default, `/.github/CODEOWNERS`); §4's inline block is flagged illustrative-only rather than claimed byte-identical (the real file has since diverged in ordering too); [RepositoryRules §7.1](../engineering-operating-system/RepositoryRules.md) and `RepositoryStructure.md` corrected identically; `.github/CODEOWNERS`'s own header comment (which flagged this exact drift) trimmed now that it's resolved. `docs-lint` PASS confirms no dangling references introduced. | ~~CODEOWNERS↔map drift~~ closed. | ~~S~~ **Done** | — | R-CA |
| DEBT-007 | TASK-0008 (MINOR-1/2/3) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep).** (Was: `scripts/docs-lint/docs_lint.py` covered the four DocumentationStandards §4 check *types* but not their full breadth.) L1 (broken links) and L2 (dangling `TASK`/`SPRINT`/`DEBT`/`RISK` id references) now cover `/work/**/*.md` under a "defined includes planned" model (an undefined id is forgiven only if numerically plausible — ≤ highest defined id of its prefix + 50 — AND not cited in a past-tense/completed context; both conditions required, calibrated against this repo's own `/work` prose). L4 gained four new named sub-checks (`L4-dlq`, `L4-nfr`, `L4-locale`, `L4-pnpm`) alongside the renamed `L4-rls-guc`. L3 gained Roadmap total-count reconciliation alongside FeatureCatalog's. 35 new tests (`scripts/docs-lint/test_docs_lint.py`). Extending L1 into `/work` surfaced one genuine, previously-invisible broken link (`TASK-0013-review.md` pointing at `FrictionMetricView.java`'s pre-TASK-0016 path) — found and fixed as part of this same pass. | ~~Incomplete drift coverage~~ closed. | ~~M~~ **Done** | — | R-DE + R-DOA |
| DEBT-008 | TASK-0009 (MINOR-1) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep) for the wording drift; the partitioning sub-item separately RESOLVED by DEBT-017's closure (same sweep, Wave 4).** (Was: DatabasePlan §7/§14 wording tension between "V1 creates all §3 tables" and "V2+ begins with Phase 1 connector additions".) §7/§14 rewritten to state V1 creates all §2 schemas plus the Phase-0 control-plane tables only; canonical business-domain tables and later-phase tables land incrementally with their owning features (matching the as-built, R-DBA-ratified reality in `work/tasks/TASK-0009.md`). The companion "plans weekly RANGE partitioning at volume" note is now also landed, not just documented — see DEBT-017. | ~~Wording drift~~ closed. | ~~S~~ **Done** | — | R-DBA + R-DE |
| DEBT-009 | TASK-0010 (MAJOR-1; MINOR-1); TASK-0013 | **✅ RESOLVED by TASK-0012.** (Was: G6 observability waiver.) The `/api/v1` read endpoints (`session`, `connectors`, `friction/summary`) shipped with no Micrometer `eip_*` metrics, no OTel spans, and no structured JSON logs, and the problem+json body carried no `traceId` ([BackendPlan §10](../docs/engineering/BackendPlan.md)). [QualityGatePolicy line 119](../engineering-operating-system/QualityGatePolicy.md) makes G6 content-triggered (adds endpoints); [line 81] permits deferral only via an R-OE DEBT waiver. `traceId` derives from the OTel/`traceparent` MDC that the observability wiring populates ([BackendPlan §10 line 86](../docs/engineering/BackendPlan.md): "populated by the tenant-context filter and OTel instrumentation"). | No metrics/traces/logs on the first live HTTP surface: request rate, latency, and 401/400 rates are invisible; incidents on these endpoints are undebuggable; problem responses lack a correlation id. Grows as endpoints multiply. | ~~S~~ **DONE** | **TASK-0012** delivered: actuator health/info/prometheus, `eip_api_request_duration_seconds` + `eip_api_requests_inflight` (tenant-safe tags), OTel tracing → Compose collector, structured JSON logs (`tenantId`/`traceId`/`spanId` MDC), and `traceId` on every problem+json. Dashboards/alerts remain in `/infra` (ObservabilityModel §5/§6). | R-OE |
| DEBT-010 | TASK-0010 (MINOR-8) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep).** (Was: `ConnectorCursor` was plain unsigned base64url with no expiry and no whitelisted `sort`.) `ConnectorCursor` (and `eip-reports`' deliberately-duplicated `ReportCursor`) now sign every cursor with HMAC-SHA256 (`eip.api.cursor-signing-key` / `EIP_API_CURSOR_SIGNING_KEY`, a dedicated key distinct from the envelope-encryption master key; `ProductionSecretsGuard` extended to refuse prod/preprod boot on its dev-fixture default), embed a 1-hour expiry, and embed the issuing `sort` (rejecting a page-2 request under a different `sort`) — constant-time signature check (`MessageDigest.isEqual`), generic rejection message never distinguishing which check failed. Whitelisted `sort` on `GET /api/v1/connectors`: `name` (default) \| `-name` \| `createdAt` \| `-createdAt`. Live-verified: signed round-trip across a page boundary, sort-mismatch → 400, tampered/unsigned cursor → 400, unknown sort → 400 naming the whitelist, prod boot with the dev-fixture key → refused. | ~~Unsigned, non-expiring cursor; no sort~~ closed. | ~~S~~ **Done** | — | R-BA |
| DEBT-011 | TASK-0010 (MINOR-4/5/6; NIT-2) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep).** (Was: only 200 responses documented; snapshot could silently drift; ~~seeder untested~~ already fixed by TASK-0016.) A global springdoc `OperationCustomizer` (`OpenApiConfig`) infers 400/401/403/404 onto every operation from the handler's existing `@RequiresPermission`/`@PermissionExempt`/`@PathVariable`/`@RequestParam` annotations, referencing one shared `ProblemDetail` schema component; 4 non-standard endpoints keep explicit `@ApiResponse`s. `backend`'s CI job gained an additive-only diff gate (regenerate via `EIP_OPENAPI_EXPORT=1`, structurally compare — `jq -S`, key-order-independent — against the committed snapshot, mirroring DEBT-016's frontend gate). **Found and fixed live**, not just in theory: the first PR-triggered CI run failed the new gate on a false positive — `Map.of()`'s per-JVM-randomized iteration order (a hash-flooding mitigation, not a bug) made a fresh CI JVM's regeneration byte-differ from a long-lived local daemon's, even though the content was identical; fixed by using a deterministic `LinkedHashMap` for the one example-value map this codebase controls, AND by making the gate itself structural (immune to any other ordering source, including one traced to springdoc's own internals, outside this codebase). OpenAPI 28→30 paths (error docs only, no new endpoints from this item alone). | ~~Undocumented errors; silent drift risk~~ closed. | ~~M~~ **Done** | — | R-BA |
| DEBT-012 | TASK-0010 (MINOR-7) | **✅ FULLY RESOLVED** — RBAC/denied-path portion by M5 Wave S1a; service tokens + claim-mapping + resource-level scope + role overrides by TASK-0025 (debt-paydown sweep). (Was: no endpoint declared an RBAC permission and no denied-path (403) test existed.) Delivered (S1a): the authoritative permission catalog (`Permission`/`Role`, `RbacMatrixTest`-locked to SecurityModel §4); `eip.security.mode` (`header`\|`oidc`); deny-by-default `@RequiresPermission`/`@PermissionExempt` + `PermissionEnforcementInterceptor` over every `/api/v1` handler; RFC 7807 401/403. Delivered (TASK-0025): **service tokens** (SecurityModel §3) — `core.service_token` (V10; ADR-025's no-RLS hybrid-table treatment, independently re-derived and endorsed on review: the auth-lookup-by-hash path must see every token regardless of tenant to discover the tenant, so no RLS predicate can scope it without breaking the lookup itself), `eipt_`-prefixed SHA-256-hashed bearer tokens, mode-independent auth filter, create/list/revoke; **deployment-level OIDC claim-name overrides** (`eip.security.oidc.tenant-claim`/`roles-claim`, honestly scoped — NOT per-tenant, chicken-and-egg reasoning in ADR-026); **resource-level team scope** (SecurityModel §4 layer 2) — `EipPrincipal.scopedTeamIds` from a JWT `eip_teams` claim, gated to manager-scope roles only (matches SecurityModel §4's own example), enforced at the composition layer before any aggregate; **tenant-editable persona role-permission overrides** — `core.tenant_role_permission_override` (V11, a delta table; `Role.permissions()` stays the fixed RbacMatrixTest-locked default), `PUT`/`GET /api/v1/admin/roles/{role}/permissions`, self-lockout guard. OpenAPI 27→31 paths across the sweep. Live-verified end to end multiple times (service-token tenant isolation + RBAC scoping + revocation; role-override effective-permission changes + self-lockout rejection). **New DEBT-024** registered (not fixed, correctly out of scope): the `audit.audit_event` write path SecurityModel §11 documents has never been built — a materially larger, separate effort. **One genuine residual remains, verified by grep before writing this row (not assumed):** the `x-eip-permission` OpenAPI extension APIDesign §10/BackendPlan/FrontendPlan describe was never implemented — `Permission.wireId()`'s javadoc references the extension name as the stable identifier a future customizer would emit, but no `OperationCustomizer` actually adds it to any operation, so `FrontendPlan.md`'s planned `<Can>`/router-guard gating off that extension has nothing to read yet (today's frontend permission gates, where they exist, are hand-wired against the same permission strings, not extension-driven). | ~~Every item this row previously listed~~ closed, except the `x-eip-permission` extension (open, see residual note). | ~~M~~ **Done** (residual: `x-eip-permission`) | The next OpenAPI/frontend-permission-gate task — add the `x-eip-permission` `OperationCustomizer` alongside the existing 400/401/403/404 one (DEBT-011) and wire the frontend `<Can>`/router guard to read it. | R-BA (R-PA: RBAC) |
| DEBT-013 | TASK-0013; TASK-0016 | **✅ SUBSTANTIALLY RESOLVED by TASK-0016 (INC-2).** (Was: friction index computed in the API read path via a placeholder `FrictionScore`, not the analytics engine; no `metric_fact` history; not the real wait-time metric.) The real metric now lands: the deterministic engine (`FlowTimeline` + `FrictionCalculator` in `eip-analytics`) computes wait-time decomposition across the correlated flow graph, projects to `analytics.metric_fact` (time series) + `analytics.rm_team_friction_current` (read model), and the API reads the read model — no longer the seed-only `rm_team_flow_current`. `FrictionScore` was deleted. **Residual RESOLVED by the TASK-0016 architecture remediation:** normalization + canonical writes moved into `eip-ingestion` (`application`/`persistence` layers), friction compute + projection + read side moved into `eip-analytics`; `eip-app` retains only ports, thin controllers, and the pipeline orchestrator. ADR-019 ownership is now enforced by `ApplicationModularityTests` (Modulith named interfaces) and `ArchitectureRulesTest` (ArchUnit). | ~~Metric logic in the composition root; no history; placeholder not the real metric~~ mostly closed; residual is only the persist wiring's home. | ~~M~~ **mostly done** | **TASK-0016** delivered the engine + projection + read-path repoint. Remaining: relocate the compute/normalize JDBC orchestration out of `eip-app` into the owning modules with DEBT-017. | R-AE (R-CA: contract) |
| DEBT-014 | TASK-0012 (review MINOR) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep, Wave 1).** (Was: `eip_api_request_duration_seconds` carried a `tenant_present` tag the source-of-record [ObservabilityModel §3](../docs/architecture/ObservabilityModel.md) catalog did not list.) §3's metric row now documents the full label set `method, route, status, tenant_present`, noting it is a bounded boolean (never the tenant id) emitted by `eip-app`'s `ApiObservabilityFilter` — verified present in the doc, not just claimed. | ~~Doc↔code drift on the API RED metric's label set~~ closed. | ~~S~~ **Done** | — | R-OE (R-DE: docs) |
| DEBT-015 | CI activation (remote) | **✅ RESOLVED directly by the TASK-0025 orchestrator (Wave 1), not delegated.** (Was: the GitHub Dependency Graph API returned 403 even for an admin, so `actions/dependency-review-action` errored and the step ran `continue-on-error: true`.) Enabled Dependabot vulnerability alerts for the repo, which unblocked the Dependency Graph API — the `dependency-graph/compare` endpoint flipped 403→200 — then dropped `continue-on-error: true` from [ci.yml](../.github/workflows/ci.yml)'s dependency-review step. **Immediate consequence found live, not theoretical:** the now-blocking gate surfaced real findings on the very next PR run (vitest RCE + 3 more CVEs) — patched, with one Windows/dev-server-only finding explicitly accepted via `allow-ghsas` with documented reasoning (see DEBT-016/018 commit `f7f2614`). | ~~Advisory, not blocking~~ closed — the dependency-vulnerability half of G3-auto is now a hard CI stop. | ~~S~~ **Done** | — | R-DOA (R-SA: supply-chain) |
| DEBT-016 | TASK-0014 (P0-E5-S1) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep, Wave 2).** (Was: `src/api/types.ts` was hand-written, not generated, with no CI check binding it to the committed OpenAPI contract.) `openapi-typescript` now regenerates `src/api/generated.ts` from the committed `backend/eip-app/openapi/eip-openapi-v1.json` via `pnpm generate:api` (`--properties-required-by-default --empty-objects-unknown`, recovering springdoc's missing response `required` semantics from Jackson's actual serialization behavior — documented field-by-field); `src/api/types.ts` is now a thin, asserted-identical re-export layer over it (no caller import path changed). CI's `frontend` job gained a blocking `pnpm generate:api:check` drift gate mirroring DEBT-011's backend OpenAPI diff gate. Verified: the frontend job is green on the latest Wave-4 CI run with this gate active. | ~~Hand-written types can drift from the contract undetected~~ closed. | ~~M~~ **Done** | — | R-FA (R-BA: contract) |
| DEBT-017 | TASK-0016 (INC-1) | **Ingestion transport is in-process/synchronous, not Kafka-async.** The [EventModel](../docs/engineering/EventModel.md) transport is Kafka (KRaft) topics `eip.*` + transactional outbox + idempotent consumers. The v0.1 friction slice runs the same logical pipeline (connector → `staging.raw_*` → normalize → canonical → correlate → analytics) **synchronously in-process** inside `SimulationIngestionRunner`, with no Kafka, outbox, or DLQ topic — the module boundaries and SPI shapes are preserved so async slots in behind them later. `staging.raw_simulation` is also not partitioned (DatabasePlan §6 plans weekly RANGE at volume; see DEBT-008). | No async decoupling, backpressure, replay-from-log, or at-least-once delivery semantics; a slow/failing normalize step blocks the ingest call. Acceptable while the only producer is a fixed, small, deterministic dataset with no real-time freshness budget. | L | **✅ CORE SPINE PAID by TASK-0021 (M2b Wave 1B):** transactional outbox (`core.event_outbox`, same-tx writes for *changed* work items only), `OutboxRelayService` (per-tenant `FOR UPDATE SKIP LOCKED`, 10-attempt derived dead-letter, `eip_outbox_*` counters), Kafka publish `acks=all` on `eip.domain.workitem` (partition key `tenantId:entityId`), idempotent consumer (dedup via `core.processed_events`, check-then-insert — partitioned-PK trade-off documented in `ProcessedEventsRepository`) triggering friction recompute, failures → `eip.domain.workitem.dlq`; app boots with the broker down (`eip.events.enabled` toggle). **Recompute debounce PAID by M5 Wave S3:** `FrictionRecomputeCoalescer` (same `eip.events.enabled` gate) — the consumer now only marks the event's tenant dirty (`ConcurrentHashMap.newKeySet`) after dedup, and the coalescer's `@Scheduled` sweep (`eip.events.recompute-delay-ms`, default 5000) drains the dirty set computing each tenant at most once per sweep, try/catch per tenant, counters `eip_events_recompute_runs`/`eip_events_recompute_coalesced`; proven by `OutboxKafkaIntegrationTest` (burst of 9 distinct events → `processed_events` = 9 while recompute runs stay < 9, read model still converges to the golden scores, and a redelivered duplicate triggers neither processing nor a recompute). **`staging.raw_*` partitioning RESOLVED by TASK-0025 (debt-paydown sweep, Wave 4):** all 7 `staging.raw_<connector>` tables converted from plain heap tables to RANGE-partitioned parents (V12), keyed on a new `first_ingested_at` anchor column — NOT the existing `ingested_at` — because PostgreSQL requires every UNIQUE/PK constraint on a partitioned table to include the partition key, and `ingested_at` refreshes to `now()` on every content-changing upsert (the everyday path), which would either silently defeat conflict detection or hit Postgres's hard refusal to `DO UPDATE` a row across partitions (empirically reproduced on a scratch PG16 instance before landing the fix). `first_ingested_at` is set once and never advanced, so a row's partition never changes after creation; `StagingRawRepository.upsertAll` pre-loads and resupplies each already-seen natural key's existing value (mirrors the existing `contentHashes()` pattern) so idempotency is preserved. 4 past + 12 future weekly partitions + a DEFAULT safety-valve partition; row-count-verified migration (raises and rolls back on any mismatch). Documented, not solved here: a scheduled partition-maintenance worker (roll the window forward, drop expired partitions) does not exist for ANY partitioned table in this schema yet (`eip-workers` has no source at all) — operationally, partitions outside the pre-created window need a manual/corrective migration until that worker is built. **Still open:** multi-stage `eip.raw.*`/`eip.canonical.*` topics (pipeline stages still run in-process behind the ports) — carries directional G5 evidence of why it matters at volume ([G5-Evidence-SPRINT-01 §5](../docs/performance/G5-Evidence-SPRINT-01.md): 100k events/h sustained NOT demonstrated; needs the reference environment). | R-IE (R-CA: EventModel) |
| DEBT-018 | TASK-0016 (INC-1) | **Connector SPI is a v0.1 subset of ConnectorFramework.** `com.eip.connectors.spi` models only the `sync → rawSink` emission path (`Connector`, `RawRecord`, `RawSink`, `SyncContext`, `Op`, `FetchKind`). The full [ConnectorFramework §3](../docs/engineering/ConnectorFramework.md) SPI (lifecycle `validate`/`testConnection`/`healthCheck`, incremental/webhook fetch with checkpoints/cursors via `core.connector_checkpoint`, secret access, schema validation) is not implemented; `RawRecord.payload` is a flat `Map<String,String>` rather than arbitrary nested JSON. | **Partially paid by TASK-0019 + TASK-0021 (M2b Wave 1A):** `testConnection`/`syncAvailable`/`ConnectorConfig`/`SyncContext.config()` landed additively; Jira, Bitbucket (PRs + reviews, artifact-only — NFR-071) and SonarQube (project + PR quality gates, never-fabricated timestamps) all ship real paginated full sync; source failures surface as RFC 7807 502 (`SourceSyncException`). Incremental fetch + `connector_checkpoint` cursors and a webhook TRIGGER path (data still flows through the connector's authenticated fetch) landed with TASK-0021 Wave 2C, plus GitHub/GitLab/Jenkins breadth (Wave 2D). **Further paid by M2b Wave 3E:** SPI 0.2 adds `Connector#descriptor()` (`ConnectorDescriptor`: displayName/description/configSchema/secretLabel) — the admin connector-type catalog (`ConnectorAdminService#types()`) is now descriptor-driven from the `ConnectorRegistry`'s installed connectors, sorted by type, replacing the hand-maintained static `ConnectorTypeCatalog` (deleted); `SourceHttp` gained a per-instance rate-limit throttle + honest `429`/`Retry-After` handling. Delete lifecycle for work items also landed (DEBT-020 item 3). **✅ Residual RESOLVED by TASK-0025 (debt-paydown sweep, Wave 2), verified in code, not just claimed:** SPI 0.3 adds `validate(ConnectorConfig)` (pure schema/semantic check, no network — `ValidationResult`) and `healthCheck(ConnectorConfig)` (defaults to delegating `testConnection`) as default methods; `incrementalSupported()` (default `false`) is genuinely wired — `RealConnectorSyncService` reads it, never checkpoint presence alone, to decide whether an `AUTO`-mode sync with an existing checkpoint may run as incremental, so a connector that never asked for a cursor is never handed one; Jira's changelog pagination (item 5) now follows the `changelog.total` overflow past the embedded ~100-entry cap via a bounded `GET .../changelog` call (10-page cap, 1000 entries/issue) merged chronologically; delete lifecycle (V9) extends past work items to `scm.pull_request`/`scm.code_review`/`cicd.build`/`quality.quality_gate` — `NormalizationService.markDeletedByNaturalKey` resolves each entity kind's `op='delete'` rows and soft-deletes them, confirmed wired for all four, not just schema scaffolding. **Still open:** nested-JSON payloads (`RawRecord.payload` stays a flat `Map<String,String>`) and delete lifecycle for any entity kind beyond these five (e.g. teams/business units, if a connector is ever built that can report their removal). | M | **First real connector task (Phase 1)** — extend the SPI to the full ConnectorFramework §3 surface (nested JSON payload; delete lifecycle for any further entity kinds a new connector introduces) as an additive change. | R-IE (R-CA: ConnectorFramework SPI) |
| DEBT-019 | TASK-0016 (INC-1) | **✅ RESOLVED by the TASK-0016 architecture remediation.** (Was: hand-rolled connection/autocommit/RLS-bind blocks in `SimulationIngestionRunner`/`FrictionPipelineRunner`/`DemoDataSeeder`, plus the read-only `TenantScopedJdbc`, risked inconsistent copies.) One reusable tenant-aware Spring transaction boundary now exists: `com.eip.tenancy.tx.TenantTransactionRunner` (TransactionTemplate + RLS GUC bind on the transaction-bound connection; transaction-local `set_config` restores state itself). All manual transaction handling was deleted; every application service and repository runs inside it; `TenantScopedJdbc` removed. Proven by `TenantTransactionRunnerTest` (unit) and `TenantTransactionRunnerIntegrationTest` (real-DB GUC/rollback/isolation/no-leak). | ~~inconsistent hand-rolled tx/bind copies~~ closed. | ~~S~~ **Done** | Delivered in the TASK-0016 remediation. | R-IE (R-PA: tenancy) |
| DEBT-020 | TASK-0016 (final review) | **Final-review MINOR/NIT backlog for the friction slice.** The independent closing review ([TASK-0016-final-review](reviews/TASK-0016-final-review.md), APPROVED 0 BLOCKER/0 MAJOR) surfaced 13 MINOR + 7 NIT hardening items. The load-bearing ones: (1) ~~`rm_team_friction_current` reads carry no `metric_version` filter~~ — **RESOLVED by M5 Wave S3:** `FrictionReadRepository#summaryRows` now pins `WHERE metric_version = FrictionDefinition.VERSION` (the same constant the projection writes — single source of truth), index-served by `ux_rm_team_friction` (EXPLAIN in [G5-Evidence-SPRINT-01 §3.2](../docs/performance/G5-Evidence-SPRINT-01.md)); regression `FrictionAnalyticsIntegrationTest#summary_reads_only_the_current_metric_version` seeds a second, legacy-version row for the same team and asserts the summary returns only the current version; (2) ~~evidence JOIN on `core.external_ref` does not pin `source_system`/`source_instance`~~ — **RESOLVED by M5 Wave S3:** `FrictionReadRepository#evidenceItems` and `InFlightRepository#items` now join through a `DISTINCT ON (entity_id)` pre-deduplicated `external_ref` derived table (deterministic pick ordered by `source_system, source_instance, external_id`) — a stable dedup rather than a source pin, because `work.work_item` carries no source-system column to pin against (documented at both join sites); regressions `FrictionAnalyticsIntegrationTest#evidence_is_not_duplicated_by_a_second_identity_source_for_the_same_entity` and `TrendsAndInsightsIntegrationTest#in_flight_items_are_not_duplicated_by_a_second_identity_source_for_the_same_entity` insert a second-source anchor for the same entity and assert no row fan-out; (3) ~~`op='delete'` raw records dead-end at normalization (no soft-delete propagation)~~ — **RESOLVED by M2b Wave 3E for work items:** `NormalizationService` resolves a staged `op='delete'` row's canonical id through `core.external_ref` and sets `work.work_item.deleted_at` (bookkeeping timestamp, no outbox event); `CanonicalWriteRepository#upsertWorkItems` clears `deleted_at` on a later re-upsert of the same identity (revival — counts as changed, emits an `upserted` event) via `markWorkItemsDeleted` + an `IS DISTINCT FROM` guard that now includes `deleted_at`; every `eip-analytics` read repository that scans `work.work_item` (`TrendRepository`, `InFlightRepository`, `FrictionProjectionRepository`, `FrictionReadRepository`) filters `deleted_at IS NULL`. No real connector emits deletes yet (v0.1 scope: work items only; other entity kinds' delete lifecycle remains open, see DEBT-018). (4) ~~nested `TenantTransactionRunner` calls rebind the GUC without restore~~ — **RESOLVED by TASK-0025 (Wave 1):** a same-tenant nested call now joins the enclosing physical transaction and skips the redundant `set_config` (the enclosing binding is reused); a nested call for a DIFFERENT tenant is explicitly rejected ("nested TenantTransactionRunner call with a different tenant is not supported"), rather than silently rebinding the GUC out from under the outer call; (5) ~~frontend null-checks assume `null` while `@JsonInclude(NON_NULL)` omits fields~~ — **RESOLVED by TASK-0025 (Wave 1):** 3 real null-vs-undefined bugs fixed in `FrictionCard` (found live, not just in theory); (6) ~~`ensureConnector` select-then-insert lacks a unique constraint~~ — **RESOLVED by TASK-0025 (Wave 1):** `V8__connector_unique_constraint.sql` adds `ux_connector_simulation_type`, closing the select-then-insert race, verified with an 8-thread concurrent test. Full list + evidence in the review file. | ~~Items 1–6 all resolved~~ closed. | ~~M~~ **Done** | — | R-AE (1–2), R-IE (3–6) |
| DEBT-021 | TASK-0022 (ADR-023); partial update M6-A (ADR-024) | **v0.1 deterministic reports skip the planned MinIO-artifact / job-runner schema.** [ADR-023](../docs/adr/ADR-023-deterministic-reports-inline-document.md): `reports.generated_report` ships with the DatabasePlan §3 DDL verbatim plus an inline `document jsonb` column; `reports.report_job`, `reports.report_template`, and `reports.report_schedule` (the planned job-state-machine + template tables) are not implemented, and rendered HTML is never written to MinIO/S3 — every `GET /reports/{id}/html` re-renders `document` on demand. `generated_by_agent` is always `'deterministic/exec-summary-v1'`; no citation or validation-agent path exists yet (`llm_call_ids`/`citations`/`validation_status` are always empty/`PENDING`). **M6-A update:** [ADR-024](../docs/adr/ADR-024-ai-explanation-layer.md) landed a STATELESS AI narrative path (`POST /api/v1/reports/{id}/narrative`, `eip-ai`'s `NarrateReportUseCase`) that reads a report's already-generated content and returns a numerically-verified narrative — but this writes nothing back onto `reports.generated_report`; `llm_call_ids`/`citations`/`validation_status` remain empty/`PENDING` exactly as before, and the job-runner/MinIO/report-template gap this row describes is otherwise unchanged. LangChain4j adoption (ADR-006) remains deferred to the later agent-runtime phase (ADR-024) — M6-A's provider clients are pure JDK `HttpClient`, not LangChain4j. | No report templates or subscription/delivery scheduling beyond the one built-in weekly cron (`ScheduledReportRunner`); no cached rendered artifact (acceptable — rendering a small deterministic document is cheap); the AI-composed report phase will need a real job-runner (queued/async generation) and MinIO wiring that do not exist today. Low risk while the only generator is synchronous and deterministic; the new narrative path is read-only and stateless, so it adds no new persistence risk. | M | The AI-composed report phase (Report Composition agent, `eip-ai`) — add `reports.report_job`/`report_template`/`report_schedule`, MinIO artifact storage, adopt LangChain4j (ADR-006), and populate `llm_call_ids`/`citations`/`validation_status` for those report types. | R-BA (R-AIA: composition) |
| DEBT-022 | M5 Wave S2 (TASK-0023, DEBT-005 side effect) | **✅ RESOLVED by TASK-0025 (debt-paydown sweep, Wave 1).** (Was: `install.sh --env preprod` unconditionally injected the dev-fixture `EIP_APP_DB_PASSWORD`/`EIP_MIGRATOR_DB_PASSWORD` and never set `EIP_SECRETS_MASTER_KEY`, so `ProductionSecretsGuard` — DEBT-005 — refused to boot.) The `--env preprod` path now provisions real, ephemeral secrets on first run (`openssl rand -base64`-generated DB passwords + master key), persists them to `.install/preprod.secrets.env` so re-running the script is idempotent, and reuses them across restarts instead of regenerating (which would orphan the already-migrated DB's password). The `--env prod` refusal message was extended to name the DB/master-key requirement alongside the OIDC one. Live-verified via a scratch-Postgres positive/negative control (the real dev stack was never touched). | ~~Preprod rehearsal crash-loops on boot~~ closed. | ~~S~~ **Done** | — | R-DOA (R-SA: secrets) |
| DEBT-023 | M6-A (ADR-024) | **The M6-A AI explanation layer ships `eip.ai.calls{purpose,status}` (`ExplainService`) instead of the already-documented `eip_llm_calls_total{tenantId,provider,model,agent,outcome}`** [ObservabilityModel §3](../docs/architecture/ObservabilityModel.md). That fuller metric anticipates the agent runtime (per-agent breakdown, `eip_llm_tokens_total`/`eip_llm_cost_estimate` for budget tracking) ADR-024 explicitly defers — shipping the documented name now would mean either a fabricated `agent` label with no agents behind it, or a name that promises cost/token tracking this slice does not implement. `eip.ai.calls` is scoped to exactly what M6-A has: which action (`EXPLAIN_DASHBOARD`/`REPORT_NARRATIVE`) was called and how it ended (`OK`/`FAILED`/`REJECTED`). | Low: `eip.ai.calls` is additive, not wrong — it just does not yet satisfy the ObservabilityModel §3 row's documented shape, so a dashboard/alert built against `eip_llm_calls_total` today would find nothing. No tenant-data or security risk. | S | The agent-runtime phase (ADR-024, LangChain4j adoption) — either rename/extend `eip.ai.calls` to `eip_llm_calls_total`'s full label set once `provider`/`model`/`agent`/token-cost tracking exist, or document both as intentionally distinct metrics if they end up serving different purposes. | R-AIA |
| DEBT-024 | TASK-0025 (debt-paydown sweep; discovered implementing DEBT-012's service-token path) | **No `audit.audit_event` write path exists anywhere in the codebase**, despite the table existing since `V1__baseline.sql` and [SecurityModel §11](../docs/architecture/SecurityModel.md) documenting a full taxonomy (hash-chained records, `category`/`action`/`actor_type`/`actor_id`/`traceparent`, a scheduled chain-verifier job, WORM anchoring). Every security-relevant action implemented so far — connector secret reveal, tenant creation, RBAC denials, and now service-token create/revoke/use — logs via SLF4J structured logging (searchable in the log aggregator) but never persists a queryable, tamper-evident `audit.audit_event` row. This was surfaced, not invented, while landing service tokens: SecurityModel §3 explicitly requires "creation/revocation/use audited," and the implementing task correctly logged the events but flagged — rather than silently built or silently skipped — that no persistence layer exists for it to write to. | Every compliance/security-review claim this repo's docs make about an append-only, hash-chained audit trail (SOC 2 CC-relevant per SecurityModel §11's control-mapping table) is currently undeliverable — there is no evidence store to produce for an auditor, and `SECURITY_AUDITOR`'s `audit.read` permission has nothing real to read. Growing risk: every new security-sensitive action (service tokens today; AI policy changes, connector secret writes, future RBAC edits) adds another SLF4J-only gap instead of a real audit row. | L | A dedicated audit-subsystem task: an `AuditService`/`AuditEventRepository` in the owning module (`eip-tenancy`, alongside `SecretsService` — check `ModuleOwnership.md` for the actual owner), hash-chaining (`prevHash`/`hash = SHA-256(prevHash \|\| canonical(record))` per tenant partition), the scheduled chain-verifier job (`eip_job_failures_total{job="audit-verify"}`), and retrofitting every already-logged security-relevant action (connector secret reveal, tenant/role mutations, service-token lifecycle) to also write through it. | R-SA (R-IE: implementation) |
