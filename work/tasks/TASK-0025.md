# TASK-0025 — Debt paydown sweep

- **id:** TASK-0025 · **sprint:** SPRINT-01 · **story:** founder directive — close out the accumulated technical-debt register
- **branch:** `feature/TASK-0017-local-install-profiles` · **model split:** orchestrator (specs/review/gates/commits/CI) + Sonnet 5 agents (all code)

## Scope

Closes: DEBT-001 (residual), 006, 007, 008, 010, 011, 012 (residual), 014, 016, 017 (partitioning sub-item), 018 (residual), 020 (items 4-6), 022.

Explicitly left open, with justification (not silently dropped): DEBT-005 residual (waits on an eip-app/eip-migrate Compose-container architecture decision not requested here), DEBT-015 (external GitHub platform toggle — attempted, reported), DEBT-021 residual (full AI-composed-report job-runner/MinIO/LangChain4j — ADR-024 deliberately defers this to a distinct future agent-runtime phase), DEBT-023 (depends on an agent runtime that does not exist yet).

## Waves

- **Wave 1 (parallel, 5 agents):** docs fixes (006/008/014), docs-lint breadth (007), install.sh preprod secrets (022), DEBT-020 items 4-6 (tx restore, frontend null-checks, ensureConnector unique constraint + V8).
- **Wave 2 (parallel, 2 agents):** connector SPI residual — validate/healthCheck, incremental capability flag, delete lifecycle for PR/review/build/quality-gate (018, V9); frontend OpenAPI codegen (016).
- **Wave 3 (sequential, 2 agents):** service tokens + x-eip-permission OpenAPI extension (012 part 1, V10); API contract hardening — signed/expiring cursor + sort param + OpenAPI error docs + additive-diff CI gate (010/011), runs after service tokens lands so it documents the new endpoints too.
- **Wave 4 (parallel, 2 agents):** per-tenant claim-mapping config + resource-level team scope + tenant-editable persona role overrides (012 part 2, V11); staging.raw_* partitioning scaffolding (017 sub-item, V12).

## Status log

- 2026-07-21 **Wave 1 DONE** (7 commits: a878bdf…1d1b8e7). DEBT-001 residual + DEBT-015 closed directly by the orchestrator (catalog aliasing; enabled Dependabot vulnerability alerts, unblocking the Dependency Graph API — compare endpoint 403→200 — and dropping `continue-on-error` from the CI dependency-review step). DEBT-006/008/014 doc reconciliation (4 agents' worth of docs fixed: CODEOWNERS location across ModuleOwnership/RepositoryRules/RepositoryStructure/CODEOWNERS' own header; DatabasePlan V1-baseline wording; ObservabilityModel `tenant_present` label). DEBT-007 docs-lint extended to `/work` (L1/L2) + 4 new L4 sub-checks + L3 Roadmap reconciliation — found and fixed one genuine, previously-invisible broken link (`TASK-0013-review.md`). DEBT-022 installer preprod secret generation, live-verified via scratch-Postgres positive/negative control (dev stack never touched). DEBT-020 items 4-6: `TenantTransactionRunner` cross-tenant nested-call guard, 3 real frontend null-vs-undefined bugs fixed in `FrictionCard`, `ensureConnector` race closed with a partial unique index + 8-thread concurrent test. Independent review + full gate run (`./gradlew check` 4m04s, frontend 97/97, docs-lint 200 files/9 checks) + live verification (V8 applied, unique index present) all green before commit.
- 2026-07-21 **Wave 2 DONE** (commits 9b3f466/f665a23/f7f2614). DEBT-018 residual + DEBT-016, plus a live-discovered pre-existing bug fix (ConnectorAdminService test/health 500→graceful FAILED) and an unplanned security pass (vitest RCE + 3 other CVEs patched, one Windows/dev-server-only finding explicitly accepted via `allow-ghsas` with documented reasoning). CI green (push + PR, incl. the now-blocking dependency-review gate).
- 2026-07-21 **Wave 3a DONE** (commits 066fb0f/0700728). DEBT-012 part 1 — service tokens, live-verified end to end. ADR-025 (no-RLS design, independently endorsed). New DEBT-024 registered (audit_event gap — correctly out of scope, not fixed). CI green.
- 2026-07-21 **Wave 3b DONE** (commits 5fcd65e/[next]). DEBT-010 (signed/expiring HMAC-SHA256 cursors, identical scheme in ConnectorCursor + eip-reports' ReportCursor; whitelisted sort on /api/v1/connectors; ProductionSecretsGuard extended for the new signing key) + DEBT-011 (OpenAPI error-response docs via a global OperationCustomizer + shared ProblemDetail schema; additive-only CI diff gate). Independent review + full fresh gate run + live verification (signed-cursor round-trip, sort-mismatch 400, tampered-cursor 400, unknown-sort 400, prod-boot-refusal naming the new var) all green before commit. First PR-triggered CI run on this wave failed the new OpenAPI diff gate on a false positive (`Map.of()` per-JVM iteration-order nondeterminism) — diagnosed and fixed by switching to a deterministic `LinkedHashMap` plus making the gate itself structural (`jq -S`); a second CI failure (gitleaks) was fixed by allowlisting the new cursor-signing-key DEV-ONLY fixture. Both fixes verified, CI green.
- 2026-07-22 **Wave 4 DONE** (commits 2853593/9319077/07d1495, pushed). DEBT-012 part 2 — deployment-level OIDC claim-mapping config (`eip.security.oidc.tenant-claim`/`roles-claim`), resource-level team scope (`EipPrincipal.scopedTeamIds`/`TeamScopeFilter`, gated to manager-scope roles per SecurityModel §4), tenant-editable persona role-permission overrides (`core.tenant_role_permission_override` V11, delta-table design, self-lockout guard) — plus DEBT-017's partitioning sub-item: `staging.raw_*` RANGE-partitioned on a new `first_ingested_at` anchor column (V12), not the existing `ingested_at`, to preserve upsert idempotency across partition boundaries (reasoning + a reproduced PG16 error documented in the migration header). Both agents stalled once mid-run (background-task watchdog, no progress for 600s) and were relaunched with adjusted prompts (smaller output increments; reason-from-documentation-first for the partition-key design rather than repeating a risky live experiment) — both completed cleanly on retry. Independent review + fresh full gate run + live verification (role-override effective-permission changes, self-lockout rejection, team-scope filtering) + OpenAPI/frontend-types regeneration (missed on first pass, caught before commit via a paths-count grep) all green before commit. Both push- and PR-triggered CI runs (29932524954, 29932527898) fully green (security/backend/docs-lint/frontend) after the fact.
- 2026-07-22 **Final debt-register.md reconciliation DONE.** Every row this task's scope names (001 residual, 006, 007, 008, 010, 011, 012 residual, 014, 016, 017 partitioning sub-item, 018 residual, 020 items 4-6, 022) updated to reflect actual delivered state, each claim spot-checked against the real code/docs (grep/read) before writing rather than assumed from memory — this caught one overclaim in a first-draft edit (the `x-eip-permission` OpenAPI extension was never actually implemented, only referenced in a doc-comment; corrected to an explicit open residual on the DEBT-012 row rather than folded into "done"). `docs-lint` PASS (203 files) after all edits. **TASK-0025 is DONE** — see Scope's "explicitly left open" list for the items this sweep deliberately did not touch (DEBT-005 residual, DEBT-021 residual, DEBT-023, plus newly-registered DEBT-024), each with its own justification already on record in `work/debt-register.md`.
