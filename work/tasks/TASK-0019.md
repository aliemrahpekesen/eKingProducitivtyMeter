# TASK-0019 — M2: Connector lifecycle SPI + real Jira connector end-to-end

- **id:** TASK-0019 · **sprint:** SPRINT-01 · **story:** founder M2 — real integrations: admin-panel config drives a REAL Jira sync into the same pipeline; Bitbucket/Sonar get real credential probes now, sync in M2b
- **branch:** `feature/TASK-0017-local-install-profiles` (M-ladder continues)
- **change class:** CC-1 (ConnectorFramework SPI extension, additive) + CC-2 (secrets in sync path)

## Delivered

- **SPI (additive, pure Java):** `ConnectorConfig` (settings + revealed secret), `TestConnectionOutcome`, `Connector.testConnection()` (default honest NOT_AVAILABLE) + `syncAvailable()`, `SyncContext.config()`.
- **Real JiraConnector** (JDK HttpClient + Jackson, zero Spring): authenticated probe against `/rest/api/3/myself` (OK only on 200; FAILED never leaks the token), paginated `/rest/api/3/search?expand=changelog` full sync emitting `work_item` + `work_item_transition` raw records — canonical states (blocked/review-aware name mapping + statusCategory), strict ISO-8601 instants from Jira's `+0000` format, project-key team attribution, unresolved issues carried without `resolvedAt`, NO person identifiers (NFR-071). Bounds: 100/page, 50 pages, first changelog page (M2b: cursors/checkpoints).
- **Bitbucket/SonarQube connectors:** REAL authenticated probes (`/2.0/user`, `/api/authentication/validate`); `sync` honestly unavailable until M2b.
- **Ingestion:** `staging.raw_jira|bitbucket|sonarqube` (V4, `LIKE raw_simulation INCLUDING ALL`, RLS enrolled); `StagingRawRepository` whitelist-parameterized per type; `ConnectorRegistry`; `RunConnectorSyncUseCase`/`RealConnectorSyncService` (config+secret resolve in a short tx → source fetch OUTSIDE any tx → content-hash idempotent batch staging); normalization reads ALL raw tables and AUTO-CREATES source-named teams under an "Imported" org/BU; `resolvedAt` now optional end-to-end.
- **Admin:** test-connection now delegates to the installed connector (real probes); catalog `syncAvailable` reflects installed implementations (jira=true); `POST /api/v1/admin/connectors/{id}/sync` composes sync→normalize→compute. Frontend: **Sync now** button on syncable connectors + result line; Bitbucket schema gained the required `username`.

## Proof (all green)

`JiraConnectorTest` (WireMock: honest probe incl. 401-no-secret-leak; mapping assertions; canonical-state vocabulary). `AdminJiraSyncIntegrationTest` (WireMock + Testcontainers, the full user journey): onboard tenant → register Jira (encrypted token) → REAL Test Connection OK → Sync now (10 records staged, PLAT team auto-created, 1 resolved item correlated) → **friction computed from JIRA data: PLAT 63, dominant REVIEW_WAIT** → replay idempotent (10 unchanged) → Bitbucket sync fails honestly (400, DEBT-018). Module ITs updated for the registry; ArchUnit split: contracts/simulation stay fully pure, connector impls may use Jackson/JDK-HTTP but never Spring/JDBC. Full `./gradlew check` + frontend 17/17 + docs-lint green. WireMock added test-only per BackendPlan §14 (version literal → DEBT-001).

## Remaining for M2b

Transactional outbox + Kafka (DEBT-017 — no longer blocks pull-based connectors: content-hash idempotent sync is safe synchronously; outbox becomes mandatory with webhooks/incremental + event fan-out), Bitbucket/Sonar sync, incremental/webhook fetch + `connector_checkpoint` cursors, changelog pagination, per-connector rate limiting/bulkheads (BackendPlan §7).
