# TASK-0023 — M5: Enterprise hardening

- **id:** TASK-0023 · **sprint:** SPRINT-01 · **story:** founder M5 — OIDC/RBAC, packaging + supply-chain hardening, performance evidence, registered-debt paydown
- **branch:** `feature/TASK-0017-local-install-profiles` · **model split:** Fable (architecture/contracts/review/gates/commits) + Sonnet 5 agents (all code)

## Scope (waves)

- **S1a — OIDC + RBAC (backend):** `eip.security.mode=header|oidc`; Spring OAuth2 resource server (Keycloak issuer, JWT: realm roles → `Role` enum, `eip_tenant` claim → tenant binding); authoritative `Permission`/`Role` catalog in eip-tenancy.rbac transcribing the SecurityModel matrix; `@RequiresPermission` + deny-by-default interceptor on every /api/v1 endpoint; RFC 7807 401/403; prod/preprod guard now requires oidc+issuer instead of refusing boot; Keycloak dev realm: eip_tenant mapper + demo SSO users; `/api/v1/session/auth` discovery endpoint (DEBT-012).
- **S1b — OIDC (frontend):** auth-mode discovery; header mode unchanged; OIDC mode = Authorization Code + PKCE via oidc-client-ts, bearer on every call, tenant from token (manual tenant input hidden), sign-in/sign-out UI.
- **S2 — packaging/supply chain:** CHANGE_ME convention + boot refusal (DEBT-005), Helm chart + air-gap notes, Gradle dependency verification metadata (DEBT-002), version-catalog consistency (DEBT-001), coverage line/branch tiers (DEBT-003).
- **S3 — performance + debt paydown:** G5 evidence (load smoke at committed envelope scale-down, EXPLAIN on hot reads, API latency measurement, documented honestly as dev-hardware numbers); recompute debounce; DEBT-020 items 1–2 (metric_version filter, external_ref source pinning); echarts code-split.

## Status log

- 2026-07-15 **DONE — M5 complete, 9 commits (502ef63…5116568 + this record).** S1: OIDC+RBAC live-verified against real Keycloak tokens (anonymous 401; VIEWER 403 naming the missing permission; ANALYST 201; spoofed X-EIP-Tenant ignored — tenant binds from the eip_tenant claim); KC24 user-profile fixture fix (email/name required) and dev direct-grants found during verification. S2: CHANGE_ME + ProductionSecretsGuard (live-smoked: prod boot with fixtures refuses, aggregated value-free message), Helm chart (lint clean, 3 template variants), verification-metadata sha256 (596 components; **Fable found the bootJar-path gap live** — stale-jar smoke exposed missing checker-qual POM entry, metadata extended to cover bootJar), catalog routing (2 documented residuals), LINE+BRANCH coverage tiers (all modules already above tier — measured before switching). S3: DEBT-020 items 1-2 paid with regressions; recompute coalescer (9-event burst → <9 computes, converged); echarts code-split (eager 1429→301 kB); G5 evidence doc (p95 42-55 ms vs 300 ms budget on dev hardware, EXPLAIN captures as RLS role, honest gap list — 100k events/h needs reference env + DEBT-008). New DEBT-022: install.sh --env preprod injects dev fixtures → crash-loops by design until the installer learns secret injection. Not pushed.
- 2026-07-15 S1a + S1b launched as parallel Sonnet agents (backend/infra vs frontend write-sets), fixed `/api/v1/session/auth` contract.
