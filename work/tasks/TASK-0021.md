# TASK-0021 — M2b: Connector breadth + event spine

- **id:** TASK-0021 · **sprint:** SPRINT-01 · **story:** founder M2b — real connector breadth (Bitbucket, SonarQube, GitHub, GitLab, CI) + the transactional outbox → Kafka event spine (DEBT-017), incremental sync + webhooks + scheduled sync
- **branch:** `feature/TASK-0017-local-install-profiles` · **model split:** Fable (specs/review/gates/commits) + Sonnet 5 agents (all code)

## Scope (waves)

- **Wave 1A — Bitbucket + SonarQube real sync** (eip-connectors only): BB Cloud 2.0 repos→PRs→activity → `pull_request`/`code_review` streams (workItemKey via `[A-Z][A-Z0-9]+-\d+` from branch-then-title); Sonar projects→gate status→PR gates → `quality_gate` stream. No person identifiers anywhere (NFR-071). Existing generic sync path (RealConnectorSyncService → staging.raw_* → NormalizationService) unchanged.
- **Wave 1B — Outbox + Kafka (DEBT-017):** outbox rows written in the same TenantTransactionRunner tx as canonical work-item upserts (EventEnvelope, UUIDv7, topic `eip.domain.workitem`, partition key `tenantId:entityId`); `OutboxRelay` @Scheduled, per-tenant FOR UPDATE SKIP LOCKED, attempts-capped dead-letter state; `DomainEventPublisher` port in ingestion.api with Kafka adapter (acks=all) in eip-app; idempotent consumer (dedup via core.processed_events) triggering friction recompute; failures → `eip.domain.workitem.dlq`; `eip.events.enabled` toggle; app boots with broker down.
- **Wave 2C — Jira incremental + webhooks + schedule:** connector_checkpoint cursors (`updated >=` JQL), webhook intake endpoint, @Scheduled auto-sync.
- **Wave 2D — GitHub / GitLab / CI connectors:** GitHub PRs+issues, GitLab MRs, Jenkins/GH-Actions builds → `cicd.build` + catalog schemas.
- **Wave 3E — SPI descriptors → dynamic catalog, SourceHttp rate limiting, delete lifecycle.**

## Status log

- 2026-07-15 **Wave 3E DONE — TASK-0021 (M2b) COMPLETE.** SPI 0.2: abstract `Connector#descriptor()`, static ConnectorTypeCatalog deleted, admin types descriptor-driven from the installed registry (sorted). SourceHttp per-instance throttle (10 rps default, monotonic clock) + 429/Retry-After honoring (30s cap, 2 retries). Webhook brute-force guard (10 rejections/10 min → 429 all-or-nothing window, `eip_webhooks_throttled`). Delete lifecycle for work items (op='delete' → `deleted_at` via external_ref, revival on re-upsert emits `upserted`, all 4 analytics repos filter). Independent gates: `./gradlew check --rerun-tasks` BUILD SUCCESSFUL 4m02s (186 tests/0 fail), frontend 37/37 + lint + build, docs-lint 149 PASS. Live: descriptor catalog 7 sorted types, friction 200, throttle verified (11th wrong-token → 429; correct token during window → 429 as designed). 9 commits total (ba0b23d…6ab3e6b). DONE. Not pushed.
- 2026-07-15 Wave 1A + 1B launched as parallel Sonnet agents with disjoint write-sets (eip-connectors vs eip-ingestion/eip-app). Fable review + independent gates pending.
- 2026-07-15 **Wave 2 DONE** (2C ∥ 2D as parallel Sonnet agents + one pipeline-gap fix agent). 2D: GitHub/GitLab/Jenkins connectors (GitLab approvals emit no code_review — no per-approval timestamps exposed, never fabricated; incomplete builds skipped), V5 staging + RLS + catalog with `webhookToken` on every real schema. 2C: checkpoint cursors (service-owned advance, failure leaves cursor untouched), Jira incremental JQL (10-min overlap), SyncMode FULL/INCREMENTAL/AUTO, webhook trigger endpoint (trigger-not-data-path, constant-time token, 30s debounce, virtual-thread dispatch), scheduled auto-sync (5 min, non-simulation ACTIVE), OpenAPI 19 paths. Fable review found + fixed via agent: webhook/scheduled paths originally staged-only — now run the FULL pipeline (normalize + recompute) so dashboards self-refresh. Independent full `./gradlew check` green (2m16s), docs-lint PASS. Live proof: V5 applied by Flyway on boot, catalog lists 7 types (all real ones sync-capable, webhookToken in schemas), webhook 401/202-accepted/202-debounced/404 + token never logged + failed sync does NOT advance cursor; successful sync writes checkpoint (FULL first pass). MINORs logged for 3E: 401/404 distinction leaks connector existence (UUIDs unguessable — acceptable), no brute-force guard on rejected webhook attempts, non-cursor connectors refetch fully under INCREMENTAL (content-hash no-op).
- 2026-07-15 **Wave 1 DONE.** 1A: BB/Sonar real sync (module coverage 89%, WireMock suites, NFR-071 asserted). 1B: outbox→Kafka spine on the exact V1 schema, changed-rows-only emission (`IS DISTINCT FROM` guard + batch counts; requires PGJDBC default `reWriteBatchedInserts=false`), per-tenant relay, derived dead-letter at 10 attempts, idempotent consumer → friction recompute, DLQ topic. Cross-wave fix (3rd agent): `SourceSyncException`→502 RFC 7807, nullable `mergedAt` for OPEN PRs, stale-javadoc sweep. Fable independent gates: full `./gradlew check` green (2m37s), docs-lint 149 PASS. Live proof on installed env: fresh tenant sample-data → 9 outbox rows → 9/9 published → Kafka envelope verified → 9 `processed_events`; replay → 0 new events; `eip_outbox_published_total 9`, dlq/dead/failed 0; catalog now reports bitbucket/sonarqube `syncAvailable:true`. Review verdicts: 1A APPROVED (MINORs: activity-order review keys, `mergedAt≈updated_on` — documented in code), 1B APPROVED (MINORs: per-event full recompute no debounce; processed-before-compute at-most-once compute — DLQ retains).
