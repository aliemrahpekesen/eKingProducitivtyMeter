# TASK-0018 — Admin Panel v1 (M1: tenant onboarding + integration management)

- **id:** TASK-0018 · **sprint:** SPRINT-01 · **story:** founder priority M1 — the installer must end in an admin panel where tenants and all integrations (Jira, Bitbucket, Sonar) are managed
- **branch:** `feature/TASK-0017-local-install-profiles` (continues the install milestone; M1 of the M1→M2→M3→M4 ladder)
- **change class:** CC-2 (security-relevant: secret storage) + additive API
- **assigned role:** R-IE + R-FA under R-PA (tenancy/secrets) + R-CA

## Scope

- **Tenancy:** `ManageTenantsUseCase` (platform-level create/list over no-RLS `core.tenant`), `ManageOrgStructureUseCase` (tenant-scoped org→BU→team tree + creates), `SecretsService` + pure `EnvelopeCipher` (AES-256-GCM envelope per ADR-014: per-secret DEK, KEK-wrapped, `core.secret`; plaintext never returned by any API; `reveal` is in-process only for the M2 sync path). KEK via validated `eip.secrets.master-key` property (dev default DEV-ONLY; prod CHANGE_ME per DEBT-005).
- **Ingestion:** `ManageConnectorsUseCase` — static type catalog (simulation/jira/bitbucket/sonarqube) with JSON-Schema config forms and honest `syncAvailable` flags (static until SPI descriptors land, DEBT-018); register (schema-required validation, secret → SecretsService), list (secrets only as `hasSecret`), status toggle, **honest test-connection** (simulation probes the real dataset; real types return `NOT_AVAILABLE` pointing at DEBT-018 — never a fake OK).
- **App:** thin admin controllers under `/api/v1/admin/*` (tenants, structure, connector-types, connectors + status/test, sample-data); `SampleDataService` (ensures sample org/BU/Platform-Payments-Web then runs the friction pipeline for the CURRENT tenant — any fresh tenant gets a computed dashboard in one click); Bean-Validation advice → problem+json; OpenAPI +10 additive paths (14 total).
- **Frontend:** header tab nav (Overview | Admin, `#admin` deep-link); AdminPanel — tenant list/create/switch, integration type cards, schema-driven config form with write-only secret input, connector list (status badge, masked secret, Test/Enable/Disable), "Load sample data & compute", org-structure tree.
- **Installer:** summary now leads with the admin panel URL and auto-opens `…/#admin` (macOS `open`/Linux `xdg-open`/Windows `Start-Process`).

## State log

- 2026-07-15 DONE. **Tests:** `AdminApiIntegrationTest` (6/6, Testcontainers, NOBYPASSRLS role, empty DB start): tenant onboarding via API + slug validation, tenant-scoped structure + RLS (tenant B sees nothing), Jira registration with envelope-encrypted token (plaintext absent from response AND from `core.secret` at rest; in-process reveal round-trips), honest tests (jira NOT_AVAILABLE / simulation OK 79 records), status toggle, one-click sample-data → friction Platform 91 for a fresh tenant with tenant-B isolation. Frontend 17/17 (AdminPanel: honest catalog badges, schema-driven form, masked secret, tenant-required state). **Live verification on this machine:** new tenant "Canli Test AS" → Jira register (hasSecret) → NOT_AVAILABLE test → sample-data (79 staged, 3 teams, 9 items) → friction 91/56/50 under the new tenant. Full `./gradlew check`, frontend gates, docs-lint green. Admin surface is unauthenticated until OIDC/RBAC (DEBT-012 extended); catalog static until SPI descriptors (DEBT-018). Not pushed.
