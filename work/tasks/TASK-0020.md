# TASK-0020 — M3: Dashboard v1 + deterministic recommendation engine

- **id:** TASK-0020 · **sprint:** SPRINT-01 · **story:** founder M3 — real multi-metric dashboard with deterministic, evidence-linked recommendations and process tracking
- **branch:** `feature/TASK-0017-local-install-profiles` · **model split:** Fable (architecture/contracts/review/gates) + Sonnet 5 agents (all code)

## Delivered

- **Backend (eip-analytics + eip-app):** data-anchored weekly trends (anchor = max(resolved_at), UTC `date_trunc('week')`, one bounded SQL, per-bucket friction via the pure `FrictionCalculator` — byte-identical for identical data); pure `RecommendationEngine` (5 documented rules: R-REVIEW-WAIT, R-BLOCKED, R-REWORK, R-FLOW-EFFICIENCY, R-AGING-WIP; thresholds in [Recommendations.md](../../docs/metrics/Recommendations.md), every rec cites its numbers in `metricRefs`); in-flight WIP view (only sanctioned `now()`, documented). 3 additive endpoints: `/api/v1/metrics/trends`, `/api/v1/metrics/in-flight`, `/api/v1/insights/recommendations`; OpenAPI 17 paths.
- **Frontend:** ECharts (6.1.0) TrendsBoard — friction/throughput/cycle-time/flow-efficiency charts, 4w/12w/26w range, validated categorical palette (light PASS+relief via always-on legend+direct labels; dark full PASS), fixed team-color identity, one-axis rule, >4 teams fold to "Other"; RecommendationsCard (severity = text+icon, never color alone; deterministic v0.1 footer); InFlightCard aging table with BLOCKED badges; honest empty states.

## Proof

Sonnet agents' gates re-run independently by Fable: `./gradlew check` all modules (analytics 97%/app 93% instr. coverage), frontend 37/37 tests + lint/build, docs-lint 149 files, OpenAPI regen. **Live on the installed dev environment:** trends Platform 91 @2026-01-05 (3 items), recommendations Platform = R-REVIEW-WAIT CRITICAL (reviewWaitPct=57) + R-FLOW-EFFICIENCY + R-REWORK, in-flight honest-empty, UI 200. Agent deviations reviewed and accepted: golden-IT arithmetic corrected by the agent (36<40 ⇒ R-REVIEW-WAIT correctly does NOT fire on the module golden set), UTC bucketing hardening, echarts bundle 445KB gzip (observation → M5 code-split candidate).

- 2026-07-15 DONE. Not pushed.
