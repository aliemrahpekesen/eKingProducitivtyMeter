# TASK-0022 — M4: Deterministic reports

- **id:** TASK-0022 · **sprint:** SPRINT-01 · **story:** founder M4 — deterministic period reports (exec flow/friction summary with trends + recommendation rationale), report library UI, print/HTML export, optional scheduled generation
- **branch:** `feature/TASK-0017-local-install-profiles` · **model split:** Fable (architecture/ADR/contract/review/gates/commits) + Sonnet 5 agents (all code)

## Scope

- **ADR-023:** v0.1 deterministic reports persist the canonical document as `document jsonb` on `reports.generated_report` (planned DDL otherwise verbatim); rendered artifacts in MinIO + `report_job`/`report_template`/`report_schedule` tables defer to the AI-composed reports phase (registered DEBT). `generated_by_agent = 'deterministic/exec-summary-v1'`.
- **Backend (Wave R1):** V6 migration (`reports.generated_report` + RLS), eip-reports module activation (api/application/persistence + pure HTML renderer), report engine composing analytics.api reads (trends, recommendations, in-flight, friction summary) into a versioned deterministic document; REST: POST `/api/v1/reports`, GET list (cursor), GET `{id}`, GET `{id}/html`; optional cron-scheduled weekly generation (default on, property-gated); OpenAPI 22 paths; docs/metrics/Reports.md.
- **Frontend (Wave R2):** Reports tab — generate (4/12/26 weeks), report library list, detail view (totals, per-team sections with trend mini-charts + recommendations), print stylesheet + "Download HTML" via the export endpoint.

## Status log

- 2026-07-15 **DONE.** R1: eip-reports module live (api/application/persistence; pure `ReportComposer` with digit-verified weighted totals + `ReportHtmlRenderer` with XSS-escape tests; keyset-cursor repository; module coverage 91%/79%), V6 verbatim-plan DDL + `document jsonb` (ADR-023) + RLS, 4 REST endpoints (OpenAPI 22 paths; operationId collision and 201-inference issues found and fixed by the agent), weekly `ScheduledReportRunner` (Mon 06:00 cron, property-gated). R2: Reports tab (#reports), generate 4/12/26w, library list, detail with per-team single-series mini-charts + reused `RecommendationItem` severity rendering + print stylesheet + fetch-and-blob HTML export; 61 frontend tests. Fable review: APPROVED (MINORs: `ReportView.completedAt` not @Nullable — always set while generation is synchronous; period reflects the data-covered span, not the requested window — data-derived, documented). Seam fix via agent: raw ISO instants in period display → `isoDateOnly`. Independent gates: backend `check` green, frontend 61/61 + lint + build, docs-lint 151 PASS. Live proof: report generated READY, document totals cross-checked against live `/metrics/trends` (itemsResolved 9=9, 3 teams), HTML export 200 with correct headers and zero script tags, list + cursor OK. Not pushed.
- 2026-07-15 Waves R1 (backend) + R2 (frontend) launched as parallel Sonnet agents with disjoint write-sets and a fixed API contract.
