# TASK-0024 — M6: Optional AI explanation layer

- **id:** TASK-0024 · **sprint:** SPRINT-01 · **story:** founder M6 — per-tenant opt-in (default OFF) AI layer that only EXPLAINS deterministic team-level results; never computes, never sees individuals
- **branch:** `feature/TASK-0017-local-install-profiles` · **model split:** Fable (ADR/contract/review/gates/commits/CI) + Sonnet 5 agents (all code)

## Scope

- **ADR-024:** LLM SPI 0.1 (semver, NFR-060) with two thin pure-JDK providers (Ollama local/air-gapped, OpenAI-compatible endpoint); LangChain4j becomes the SPI's default impl when RAG/agent phases land. Narratives are stateless (nothing persisted but a hash-only `ai.llm_call_audit` row — prompts embed tenant metrics, so bodies are never stored/logged). Numeric cross-check gates every narrative: any number not present in the deterministic source rejects the output.
- **M6-A (backend):** V7 (`core.tenant_ai_policy` + `ai.llm_call_audit`, RLS); eip-ai module (spi/providers/api/application/persistence); prompt composer over the SAME analytics/reports query ports the UI reads; RBAC additions `ai.policy.manage` + `ai.agent.invoke`; endpoints: ai/policy GET+PUT, ai/status, insights/explain, reports/{id}/narrative; RFC 7807 409/502 mappings; OpenAPI 27 paths.
- **M6-B (frontend):** Admin "AI explanations" policy card (write-only API key); "Explain with AI" on Overview + "AI narrative" on ReportDetail — rendered only when enabled, always labeled AI-generated with the fixed disclaimer, excluded from print.

## Status log

- 2026-07-21 **DONE.** M6-A: eip-ai module (25/25 module tests, 94% line coverage), V7 + RLS, RBAC additions, 4 endpoints (OpenAPI 27), circular-dep deviation resolved via `ReportNarrativeInput` mapped in eip-app (eip-ai must not depend on eip-reports — BackendPlan §1). M6-B: fail-closed UI (95/95 tests), write-only key, print-excluded narratives. Fable independent gates green (backend full check, frontend 95/95 + lint + build, docs-lint 155). Live end-to-end against a mock OpenAI-compatible endpoint: default OFF → 409; policy enable with secret; valid narrative 200 (cited [3,50,56,91] + verbatim disclaimer); invented number → 502 ai-rejected; **the checker also caught a plausible-but-absent aggregate ("9 total items" — only per-team 3s exist in the source), proving the guardrail live**; report narrative 200; audit rows hash-only (sha256 + char counts, no content). Policy restored to OFF. DEBT-023 filed (metric-name reconciliation with ObservabilityModel's fuller llm_calls shape).
- 2026-07-21 Push + CI: feature & integration pushed (first CI round failed: gitleaks fixture findings + verification-metadata buildSrc gap — both fixed, `ea93bcd`); CI re-run in progress. M6-A + M6-B launched as parallel Sonnet agents with the fixed AI API contract.
