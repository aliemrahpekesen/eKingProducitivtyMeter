# preprod mode (vite build --mode preprod). No default tenant — operators enter their tenant id in
# the UI (fails closed otherwise). Proxy target points at the preprod backend.
VITE_EIP_TENANT_ID=
VITE_API_PROXY_TARGET=http://localhost:8080
