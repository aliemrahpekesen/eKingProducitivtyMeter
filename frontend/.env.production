# production build defaults. No default tenant, no localhost assumptions baked in — the serving
# layer injects the API origin; tenant identity arrives via OIDC once DEBT-012 lands.
VITE_EIP_TENANT_ID=
