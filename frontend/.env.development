# dev mode (vite dev server default). Public values only. Demo tenant is preloaded so the UI works
# without a DB lookup; the proxy target follows the local backend.
VITE_EIP_TENANT_ID=00000000-0000-4000-8000-0000000000de
VITE_API_PROXY_TARGET=http://localhost:8080
